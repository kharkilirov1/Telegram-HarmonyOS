# STATUS — Telegram-HarmonyOS

Snapshot date: 2026-07-22 (target API 26; live runtime floor API 23)

## Latest (2026-07-22, ночь) — Island Player Suite v2+v3: системная интеграция и полный плеер

- **v2 — AVSession + фон:** `GlobalMediaPlayback.attachAvSession(context)` (вызов из MainTabsPage.aboutToAppear) создаёт одну сессию 'audio' на приложение; на каждом снапшоте — троттленный `setAVPlaybackState` (флипы play/pause или ≥3с позиции), на смене трека/меты — `setAVMetadata` (assetId=messageId, title, artist, duration, mediaImage при наличии обложки). Команды из системы: play/pause→toggle, stop, playNext/playPrevious→findAdjacentAudio, seek→seekToProgress. Continuous task AUDIO_PLAYBACK берётся на старте воспроизведения (wantAgent на EntryAbility), отпускается при полном стопе; module.json5 — backgroundModes audioPlayback + KEEP_BACKGROUND_RUNNING (+reason-строки en/ru/zh). **E2E:** системная медиа-карточка в Пункте управления с треком/исполнителем/бейджем приложения (`control-center-media.png`), музыка играет спустя 12с после Home (`curState: playing`), pause из системной карточки доходит и исполняется (`avSession cmd: pause` → JsPause Task).
- **v3 — карточка плеера:** тап тела острова с музыкой открывает `TgIslandPlayerCard` в нативном `bindSheet` (560vp, showClose): обложка (заглушка при отсутствии; `album_cover_thumbnail` допарсен DTO→модель(AppState)→VO, недокачанный thumb догружается по coverFileId и подставляется), тайтл/исполнитель, слайдер-сик с временем (E2E: 0:13→5:09 драгом), контролы 1x/1.5x/2x (`setPlaybackSpeed`, скорость переживает смену трека), ⏮/▶⏸/⏭ (prev-E2E: Mhk'→Stand up friend'). Войс-тап остался прыжком к сообщению. `findNextAudioNewer` обобщён в `findAdjacentAudio(newer)`.
- Хвосты-полиш в TODO: bg task стоп/старт на межтрековом '0'-снапшоте; строгие play/pause вместо toggle для системных команд; обложка в системной карточке.

## Ранее (2026-07-22, вечер) — Island Player Suite v1: полоса по паттерну Музыки

- **⏭ для музыки в острове:** `TgRootMiniPlayer` получил skip-next (новая иконка `ic_skip_next`), показывается только для `contentType==='audio'`. Глобальный next в `GlobalMediaPlayback.playNextAudio()`: `searchChatMessages(from='0', filter=Audio, limit=100)` → ближайший новее якоря → при нужде блокирующий `downloadFile(synchronous=true)` → toggle. Тот же метод подхватывает auto-advance аудио, когда страница чата мертва (раньше completed вне чата глох). Конец плейлиста = честный no-op.
- **Тап тела плеера → прыжок к играющему сообщению** (voice и audio; в v3 audio заберёт морф-карточка): `PendingJumpSignal` + tick-мост `islandOpenChatTick/Id` из MainTabsPage в живой ChatListPage, который выполняет свой полный `openChat`-ритуал. Голый `pushPathByName` из таб-страницы рендерит сплит-плейсхолдер «Чат не выбран» — открывать чаты можно только ритуалом ChatListPage (активный чат в трёх стейтах + unread-снапшот + requestOpenChatData).
- **Витнессы (канал «Ремиксы | Gold», API23):** остров с музыкой `[⏸ Mhk' / Ремиксы | Gold ⏭ ✕]`; ⏭ со второго трека переключил на новейший (hilog `playNextAudio: next=… local=true` + скрин); ⏭ на последнем треке — no-op с «no newer track»; body-tap открыл канал прямо на играющем бабле. Скрины в `.codex/ui-audit/2026-07-21/root-bar-adaptive/` (island-music-next, next-switch-final и др.).
- **Пофикшенный silent-fail:** file id из TDLib-дерева обязан извлекаться `getTopLevelNumber('id')` — generic `getNumber` ловит `remote.id` (строку) и молча даёт 0 (грабля уже была задокументирована в parseSharedMediaItem; LESSONS 114).
- Спека пласта: `docs/superpowers/specs/2026-07-22-island-player-suite-design.md` (v2 — AVSession+фон, v3 — морф-карточка).

## Ранее (2026-07-22) — остров-мини-плеер (срез 3/3), авто-мини выключен

- **Воспроизведение войсов/аудио стало app-глобальным:** новый `GlobalMediaPlayback` (pages/chat) владеет единственным `MediaPlaybackController`+AVPlayer на всё приложение; страница чата больше не создаёт/не убивает плеер на вход/выход, а только регистрирует row-repaint хендлеры (`setPageHandlers`, с реплеем последнего снапшота для мгновенной отрисовки строк при реоткрытии). Выход из чата НЕ останавливает звук — им владеет остров (поведение Telegram iOS). Зеркало `@ObservedV2 MediaPlaybackUIState` (hasActive/isPlaying/progress/title/subtitle/chatId) питает UI.
- **Мини-плеер в HDS miniBar-слоте острова:** новый атом `TgRootMiniPlayer` (play/pause 48vp — он же контент свёрнутой линзы, тайтл+сабтайтл, стоп-✕, 2vp прогресс-полоса по нижней кромке); арбитраж слота в `MainTabsPage` — активный/закрывающийся Search всегда важнее, плеер возвращается после; `rootIslandMiniBar()` объединяет обе ветки, ширины/inset обобщены на все root-табы. Мета публикуется страницей на всех 4 стартовых путях (`publishNowPlayingMeta`: voice → отправитель+«Голосовое сообщение», audio → audioTitle+отправитель).
- **API23 E2E-витнесс (через Shared Media → Voice, т.к. скролл-охота по прыгающему списку ненадёжна):** play в канале «Егор Настоящий» → Back → остров показывает `⏸ Егор Настоящий / Голосовое сообщение ✕` с бегущей синей прогресс-полосой; ✕ останавливает и возвращает табам слот. Скрины: `.codex/ui-audit/2026-07-21/root-bar-adaptive/island-player-{active,final}.png`. Обе smoke и main+ohosTest сборки зелёные.
- **Пофикшенный подводный камень:** `openAndStart()` начинается с `release()`, чей снапшот `messageId='0'` затирал только что опубликованную мету (пустая шапка плеера, witnessed) — мета теперь буферизуется во wrapper'е и повторно применяется на каждом ненулевом снапшоте.
- **Дефект линзы ПОЧИНЕН (2026-07-22):** плеер больше не рисуется поверх таб-бара. Root cause оказался трёхслойным (систематика по hilog-инструментации + bounds-дампам): (1) prop `miniBarStyle=EXPAND`, отрендеренный пока бар скрыт, делал поздний `applyMiniBarStyle(EXPAND)` бездельтовым no-op — введена защёлка `playerBarStyleApplied` (prop не опережает транзакцию, search-паттерн «флаг+apply в одном кадре на видимом баре»); (2) транзакция, выпущенная синхронно из монитора `rootBarHidden` (внутри animateTo показа бара), вкладывалась в чужую анимацию (mode=0 вместо APP_TRIGGER=2) — отложка на `ROOT_TAB_BAR_TRANSITION_SHOW_MS+30`; (3) ручной `barWidth=48` prop при видимом плеере заставлял HDS давить РАЗВЁРНУТЫЙ таб-бар в 48px (пять табов вертикальными колонками букв под плеером) — prop всегда полная ширина, 48vp-линзу одного таба создаёт только style-транзакция (tab 269.7→48). Витнессы: hilog `mode=2` + `miniBarAnim/tabBarAnim`, скрины `island-player-lens-done.png` (линза+плеер в ряд), `island-player-paused-final.png` (pause-тап флипает ▶ — хиты чисты), stop возвращает табы, search-регресс зелёный. LESSONS 113.
- **Авто-мини при скролле выключен** (`USE_ROOT_BAR_AUTO_HIDE_ON_SCROLL=false`) по решению пользователя — остров постоянный до появления настроек.
- **Инцидент окружения:** temp-файл qemu эмулятора раздулся до ~66ГБ и забил хост-диск C: до нуля — падали и сборки (`MergeProfile 00308018` = нет места), и сам эмулятор (после крашей temp освободился); дополнительно скрытые провалы `hdc install` из-за места на эмуляторе (см. LESSONS 110–112). Диск освобождён, эмулятор восстановлен CLI-стартом, живая сессия уцелела.

## Latest (2026-07-21, вечер) — адаптивный root-остров, срезы 1–2 из 3

- **Скрытие/показ root-острова теперь анимированные, а не покадровый степ:** все входы (`chatScreenVisible`, `keyboardVisible`, `selectedIndex`, режим сплита, поисковые флаги) сведены через `@Monitor` в один `@Local rootBarHidden`, который меняется внутри `animateTo` (`220ms EaseOut` показ / `180ms EaseIn` скрытие); атрибуты `HdsTabs` (`barHeight`/`barOpacity`/`barBottomMargin`/mask) читают только его. `resolveRootTabBarRenderStateFromHidden` вынесен чистой функцией, старая сигнатура сохранена для тестов/контрактов. `navState` переведён в `@Local`-ссылку на `@ObservedV2`-синглтон — паттерн `@Local` + `@Monitor('navState.chatScreenVisible')` подтверждён доками ArkUI.
- **Авто-мини острова при скролле чат-листа (решение пользователя, позже станет настройкой):** гистерезис по направлению (`ROOT_TAB_BAR_AUTO_HIDE_THRESHOLD = 24vp` аккумулированного одного направления) в `onScrollFrameBegin`, latch-free событие `onRootBarAutoHide` наверх, дедупликация и guards в `MainTabsPage` (не в сплите, не при поиске, сброс при входе в чат/смене таба/активации поиска, принудительный показ на `onReachStart`). Флаг `TgUiFeatureFlags.USE_ROOT_BAR_AUTO_HIDE_ON_SCROLL` — будущий переключатель настроек. Первая итерация само-прятала остров на стартовых rebuild-штормах — закрыто фильтром `ScrollState.Scroll|Fling` (только реальный жест).
- **Runtime E2E на API23 (fresh install, живая сессия):** после стартовых штормов остров видим (`Контакты`=1 в дампе), свайп вниз — скрыт (`0`), свайп вверх — вернулся (`1`); диагностические логи подтвердили полный путь emit → handle → sync → рендер до снятия. Оба smoke и чистый `assembleHap` зелёные. Промежуточный кадр самой анимации инструментально не пойман (скринкап-бёрст разминулся с 200мс-переходом) — плавность принимает пользователь вживую; свидетельства состояний: `.codex/ui-audit/2026-07-21/root-bar-adaptive/`.
- **Инфра-урок, стоивший часа:** `hdc install` при нехватке места на эмуляторе печатает ошибку И `AppMod finish` — усечённый вывод маскировал провал, рантайм крутил старый код при «зелёной» цепочке build→install→restart (см. LESSONS 110). Диск освобождён (~28MB tmp-мусора прошлых сессий; HAP теперь 158MB из-за вендоренного нативного слоя).
- **Срез 3 (мини-плеер войсов в miniBar-слоте) — следующий:** дизайн зафиксирован — арбитраж слота (активный поиск > плеер), показ на всех root-табах, play/pause + тайтл, `MediaPlaybackController` уже глобальный; ограничение платформы — слот `miniBar` у `HdsTabs` один.

## Latest (2026-07-21)

- **Недельный незакоммиченный пласт (13–19 июля) разложен в 12 тематических коммитов** (`be07eef..6b1894e`): chore/gitignore → native rlottie → core contracts → domain use cases → resources/tokens → chatlist+root shell → chat timeline → composer/entity keyboard → Compose → profile/forward/surfaces → smoke → docs. Рабочее дерево было полностью чистым после разбивки; мусорные `git_*.txt` дампы удалены, `third_party/rlottie` (2.1M, 116 файлов) вендорен по подготовленному `.gitignore`, `tdlib` (691M) остался игнорируемым.
- **Баг-хант баблов/цитат: 11 дефектов найдено, 10 исправлено в этом срезе.** Исправлено: (1) entity-форматирование (bold/italic/code/link-цвет) теперь рендерится внутри quote-сегментов `TgTextBodyV3` вместо плоского Text; (2) accent-бар цитаты прижат к левому краю фона с зазором `MSG_QUOTE_BAR_GAP` перед текстом (раньше зазор был снаружи бара, текст прилипал к бару); (3) `TgReplySnippet` переведён со Stack+вычисленной высоты бара (жёстко 1+2 строки) на Row + `LayoutPolicy.matchParent` — полоса всегда совпадает с фоном при 2-строчном тайтле/3-строчном превью quote-реплаев; (4) снята двойная ratio-обрезка ширины сниппета (движок резервировал полную ширину, рендер повторно применял 0.85/0.65 к уже ужатой ширине — на regular-ширинах сниппет резался до ~42–65%) — все 4 продакшен-вызова передают `maxWidthRatio: 1.0`; (5) `captionEntities` доведены до `TgMediaBubbleShellV2` (Router их получал, но shell хардкодил `[]` в quote-ветке капшена); (6) inline-мета больше не накладывается на полноширинный фон завершающего quote-блока — движок принудительно выносит мету отдельной строкой; (7) reply на сообщение-с-цитатой больше не помечает НОВЫЙ черновик как quote (флаг цели протекал в баннер композера; send-путь не затронут — он шлёт только `reply_to_message_id`); (8) `textQuote.position` парсится из актуального TDLib-контракта (позиция 0 валидна; прежний `quote_offset > 0` всегда давал −1); (9) локализованы англ. литералы reply-слоя: fallback-автор `Reply`, `You`→«Вы», `Unknown`→«Неизвестный», generic `Message`→«Сообщение» — включая гвард `refreshRestoredReplySnippet`, который сравнивал с литералом `'Message'` и на русской локали никогда не ре-резолвил восстановленный драфт-чип; (10) добиты 5 недостающих строк long-press меню в ru_RU/zh_CN (`action_reply/edit/copy/pin/delete` были только в base — меню действий было английским). Ключи ForEach quote-сегментов теперь включают данные (правило №10). Не чинилось: collapsed-цитаты рендерятся приглушением без expand-каретки (отдельный срез).
- **Скрытая краснота on-device сьюта закрыта:** первый полный прогон после срезов 18–19 июля показал `580` тестов и 4 фейла; два — известные несвязанные (URI-схема link preview, pinned-thumbnail), два — реальные незадокументированные: `resolveRefreshedMediaGalleryState` читал `sourceMessageId` без фолбэка (пустой id утекал бы в geometryTransition). Добавлен фолбэк на `messageId`; финальный прогон `580 run / 578 pass / 2 known fail`. Добавлены 3 фокусных теста парсинга `textQuote` (текст, position, position=0, plain-reply). Витнессы: оба чистых билда (`assembleHap` main 1:36, ohosTest 0:47), smoke-контракты, on-device Hypium `578/580`, скрин живого чата на новой сборке `.codex/ui-audit/2026-07-21/bubble-quote-fixes/01-file-chat-post-fix.png` (fresh PID `5740`). Runtime-скрин именно quote-бабла — в догфуд (в досягаемой истории цитатных сообщений не нашлось).
- **Новые наблюдения в бэклог:** сабтайтл Saved Messages (`File`) показывает «группа»; non-quote ветка капшена в media shell рендерит плоский Text без entities (в quote-ветке entities теперь есть); `hdc` из Git Bash требует `MSYS_NO_PATHCONV=1` (см. LESSONS).

- **The current-iOS bottom Search dock follows the Apple-design direct-response/reversible-motion contract on native HDS:** one ArkUI `Search` remains mounted while collapsed, its physical tap owns focus/IME, and HDS owns the material transition. Each touch-down restores window-wide `KeyboardAvoidMode.RESIZE`, fixing the reproduced second-open overlay. A fresh light API24 HAP passed `idle -> active -> close -> active -> close`: both activations focused Search at `[73,1526][1079,1694]`, resized the app root to `[0,137][1320,1799]`, and raised the system keyboard; both closes restored the idle `[1085,2583][1247,2751]` lens and full root without navigation. WindowManager exposes localized `Закрыть поиск` as one grouped `button`; spoken narration still needs a human pass. Evidence: `.codex/ui-audit/2026-07-19/root-search-dock/api24-repeat-resize-final/`. Focused contract, both shell smokes and clean main `assembleHap` pass; runtime HAP SHA-256 `FDE98410BD23660ACF8FDFBC80A49A2C147C88444FE972490085C027325713BB` after an observed no-process interval, final clean artifact `CE9881CC6114BA280C8DA7F1748D3A363165D8F4195AAE5871C5AEAB52BBD3AF`.
- **ChatList Compose now uses real contacts plus live cloud/global user search rather than recycling the dialog picker:** native Search keeps immediate local filtering, then typed TDLib `searchContacts` / `searchChatsOnServer` / `searchPublicChats` results are hydrated as private users, deduped in local → cloud → known → public order, self/deleted peers are rejected, and iOS-style `Контакты` / `Глобальный поиск` sections appear only when populated. A generation/lifecycle guard rejects stale q1 results; store-driven contact rebuilds are coalesced to 300 ms and hydration is capped before `getChat/getUser`, fixing the reproduced `THREAD_BLOCK_6S` cold-search failure. API23 survived the same cold `durov` query with no new fault, rendered five global rows, returned cached results in about 0.9 s, showed localized no-results, restored local contacts on clear, and a real q1 `durov` → q2 `telegram` overlap ended only with q2 sections. Selecting a global row sent `createPrivateChat` and opened its returned private chat; Back restored ChatList/HDS. Evidence: `.codex/ui-audit/2026-07-19/compose-remote-search/after-fix/`. Focused contracts, both shell smokes, clean main `assembleHap` and ohosTest `OhosTestCompileArkTS` are green; on-device Hypium ran `546` tests (`544` pass, the same two unrelated URI/pinned-thumbnail failures). Final HAP SHA-256 `8AD468EC28EF4D019C7051A3350018443226959893CFC1828E5AF6C0FEB2C572`, fresh PID `16076` after an observed no-process interval.
- **Compose Group → Contact → Channel is now a functional, typed first slice instead of visible no-op parity:** group selection/info calls `createNewBasicGroupChat`, contact import distinguishes registered and unregistered outcomes before `createPrivateChat`, and the bounded private-channel form calls `createNewSupergroupChat`. Committed results survive hydration/navigation failure without repeating a server mutation; the UI caps groups at 200 members and surfaces partial member failures. API23 proved action order, group selection/info, contact system-keyboard focus, top-aligned channel intro/form, direct Channel Back → Compose, Compose → ChatList and clean reopen. No valid Create/Done submission was made during the witness. Evidence: `.codex/ui-audit/2026-07-19/compose-actions/after/`. Four focused contracts, both shell smokes, clean main `assembleHap` and ohosTest `OhosTestCompileArkTS` are green. Final HAP SHA-256 `4C2B45A8EB634F5BBD516F5C130428B895667907D65DE35566B62967AD8B5FC9`, fresh PID `4309` after an observed no-process interval. Full iOS parity still requires group global search/tokens/avatar/TTL, OS Contacts/country/phone-resolution paths, channel visibility/username/subscribers and a shipped-runtime Compose capture.
- **M4 exact multi-pin collection and navigation are now live rather than a fake `0 / 1` panel state:** typed `searchChatMessages(searchMessagesFilterPinned)` preserves TDLib `total_count` and the string `next_from_message_id`, the chat page owns exact stripe index/count and paged collection state, and the iOS-style list enters/exits on an interruptible right-edge spring. API23 opened real `HUAWEI Chat` with `2548` pins, rendered the newest and numbered older rows, selected `#2547` into the panel and jumped to its exact `HUAWEI готовит ноутбук...` message. Ten real list swipes reached visible `#2434...#2421`, proving pagination beyond the first 100 results. Evidence: `.codex/ui-audit/2026-07-19/pinned-messages-navigation/`. Focused policy, both shell smokes, clean main `assembleHap` and ohosTest `OhosTestCompileArkTS` are green; installed HAP SHA-256 `58ECA40F97ABF9B9DF7676E986115BF8A3D54A2EAB4C15738A7591EFE56EF1C9`, fresh PID `26981` after an observed no-process interval.
- **M4 grouped-photo albums now follow the current iOS per-member contract rather than a fake four-cell/count template:** the real 2–10 aspect-ratio mosaic uses the iOS `1vp` seam, gallery transfer chrome is `50vp`, a remote ordinary photo requests only its exact file and stays in chat, and gallery refresh follows the current member source identity instead of the originally tapped cell. API23 showed an eight-cell `File` album, isolated the active request to one remote cell while its sibling stayed idle, logged `fileId=11977`, then captured an exact local-cell source → intermediate → gallery `1 / 10` → reverse-return sequence. Evidence: `.codex/ui-audit/2026-07-19/grouped-photo-album/final-acceptance-v2/`. Focused contracts, both shell smokes, clean main and ohosTest builds are green; the latest HAP SHA-256 is `ABD3B64F7F828BD98BA65B74A89345F7ACDD2EC97DBBB8EDD6701A25D782F732`, fresh PID `26756` after an observed no-process interval. Isolated cancel/retry remains open because the available media completed before the second tap.
- **The Apple-design interaction rubric is now a reusable local skill and has already been applied to the gallery/TGS verification loop:** the skill requires direct-manipulation state machines, native platform grounding, reversible motion and runtime witnesses instead of screenshot-only acceptance. For the current visible-TGS slice, `@ohos/lottie` was upgraded from resolved `2.0.29` to stable `2.0.32`; API23 proves the real `zai` sticker is visible and animated, the looper already runs at `30 HZ`, and leaving the chat deactivates/destroys it with `0` Canvas updates and `0` app VSyncs in a 3-second offscreen trace. Visible traces remain effectively unchanged (`62/62 -> 60/57` Canvas/VSync; host-core mean `2.425 -> 2.371`, only `-2.2%` and within run noise), so no CPU improvement is claimed and the P1 stays open. Evidence: `.codex/ui-audit/2026-07-19/visible-tgs-pacing/`. Focused lifecycle contract, both shell smokes and clean main build pass; installed HAP SHA-256 `E1892670F26AD6240C7E1A7B148467D5E91830A286C55C21DEEB647AFB1A5722`.
- **The Telegram/Nekogram `rlottie` renderer was ported as a measured opt-in experiment, not silently promoted to production:** a separate `tg_rlottie` NAPI module now owns create/render/destroy, ArkTS drives it with native `DisplaySync` at 30 Hz, reuses one `ImageData` buffer, and stops/destroys offscreen. API23 proves correct pink/cyan/red pixels, changing live frames, `511x511` physical render size and zero offscreen Canvas/VSync work. The experiment nevertheless averaged `3.736` host cores versus `2.371` for the current `@ohos/lottie` path, so `TGS_NATIVE_RLOTTIE_ENABLED=false` keeps the measured winner as the production default. A clean rebuild/reinstall then logged `@ohos/lottie 2.0.32` at 30 Hz with no native-create marker; HAP SHA-256 `477139BA2A3C142D96E812B1034D130731D000667E9FF3C121069DA6FF38C373`. Evidence: `.codex/ui-audit/2026-07-19/native-rlottie-tgs/`; the remaining P1 is a direct native surface/XComponent path that avoids per-frame Canvas upload.

## Latest (2026-07-18)

- **Photo gallery gestures now follow the Apple/iOS direct-manipulation contract with live ArkUI arbitration:** single/double taps are exclusive with double first; double tap zooms around the local point; pinch preserves the grabbed point while its center moves; all offsets are bounded to contained-media overflow. Dismiss and zoom pan are separate tagged recognizers selected from the current scale by `onGestureRecognizerJudgeBegin`, so `1x` horizontal motion reaches `Swiper` while zoomed motion pans the photo. API23 proved `17 / 76 -> 18 / 76` at `1x`, zoomed pan staying at `17 / 76` with crop MAD `69.0407`, exact reset MAD `0.0`, and reset-scale vertical dismiss. Evidence: `.codex/ui-audit/2026-07-19/gallery-gesture-arbitration/`. Both shell smokes, clean main and ohosTest `assembleHap` are green; installed HAP SHA-256 `409347C11AB014E81003E2E69E44F1C569AE5984C0BC5E678E3316F322E39005`.
- **Media gallery open/close now keeps the ArkUI shared element alive for the full spring transaction:** the source media node and active fullscreen item retain the same message-derived `geometryTransition` id and both add `TransitionEffect.OPACITY`, as required by the official ArkUI `geometryTransition + animateTo` contract. API23 captured source, an actual intermediate morph frame, fullscreen destination, and return to the single visible source; the intermediate frame shows the video geometry expanding while the chat stays behind it rather than jumping to a finished fullscreen layout. Evidence: `.codex/ui-audit/2026-07-18/remote-video-gallery/24-geometry-open.gif`, `geometry-open-frames-214314551/witness_01_mid.png`, and `26-geometry-close-final.png`. Both shell smokes and clean main `assembleHap` are green; HAP SHA-256 `3AD782D1BD8D3CD6C7F8F2997265200ED6C2E17F6F8231941BBA5F9C10E24815`, fresh PID `24602` after an observed no-process interval.
- **Remote video taps now enter the gallery immediately instead of waiting behind the chat bubble:** `TgVideoBubble` starts an idle download once and opens the existing viewer in the same tap; an already-active transfer opens without a duplicate request. Gallery items now carry TDLib `minithumb` separately from the full local path and local thumbnail, so the pending fullscreen state can keep a blurred preview under transfer controls. API23 captured the intended path engaged: `fileId=12085` was requested while gallery item `8 / 10` was already open with `LoadingProgress [598,1384][710,1496]`; the final clean HAP then reopened the downloaded `0:26` video at the same index. Evidence: `.codex/ui-audit/2026-07-18/remote-video-gallery/`. Both shell smokes, clean main `assembleHap` and clean `OhosTestCompileArkTS` are green; final HAP SHA-256 `FC76F1468B0AD2215704A5A70E8F35A48ACAFF37A9C3BBDB88A54536F269BB37`, fresh PID `11772` after an observed no-process interval.

## Latest (2026-07-17)

- **ChatList no longer presents bots as ordinary online users:** `ChatItemVO` now resolves role before raw presence and withholds `avatarIsOnline` for bots, matching the supplied shipped-iOS `zai` row while leaving ordinary online-user behavior intact. On the same API23 row the green node `#FF4DCC5E [189,1060][238,1109]` disappeared with identical avatar `[35,906][245,1116]` and title bounds. Evidence `.codex/ui-audit/2026-07-17/chatrow-bot-online-indicator/`. Both shell smokes, clean main build and `OhosTestCompileArkTS` are green; final clean HAP SHA-256 `2E525E42ECD66652A1B7F78C3B8109F38595279DFEF025F4D357D21A5AAA5596`, fresh PID `23344` after an observed no-process interval.
- **Chat-title subtitles now follow Telegram iOS peer semantics and the active app language:** the page no longer hardcodes English presence/activity copy, bots are classified before presence and render the localized bot role, and missing Russian/Chinese channel/group/subscriber plus activity strings are now complete. On the same API23 Russian session, live `zai` changed `online -> бот` (`[597,215][714,264] -> [621,215][690,264]`) and the no-count broadcast changed `channel -> канал` without changing the native/hybrid glass geometry. Evidence `.codex/ui-audit/2026-07-17/chat-topbar-localized-subtitle/`. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; final clean HAP SHA-256 `2B00BA9DE44B0B35F9D5D3088932173F3809D7062182F554E1904CA67532F363`, fresh PID `9953` after an observed no-process interval.
- **Special-message bubbles now obey the same Telegram iOS grouped geometry as text and media families:** contact, location and poll no longer force a standalone incoming radius; all use the timeline `bubbleRadius()` resolver for `top / middle / bottom`, matching iOS's shared `ChatMessageBubbleItemNode` ownership and contact `forceFullCorners: false`. The reusable `TgBubbleTail` also moved from deprecated global `vp2px` to instance-bound `UIContext.vp2px` without changing its `8vp x 14vp` result. API23 rendered both visible outgoing voice tails at exactly `28x49px` and the mirrored incoming tail remained present; evidence `.codex/ui-audit/2026-07-17/grouped-bubble-geometry/01-runtime.jpeg` + `.json`, `02-final-clean.jpeg` and `runtime-metrics.json`. Both shell smokes, clean main `assembleHap` and ohosTest `assembleHap` are green; the final clean HAP was installed and launched as PID `24129` after an observed no-process interval, SHA-256 `4F5AF183BEC26B82D0C8363EA4B98BCB8E10F5D9A315EB43CD39AFF6A30DCB51`. The old `TgBubbleTail vp2px is deprecated` build warning is gone.
- **The intermittent group-chat unread divider is now deterministic and matches Telegram iOS geometry:** ChatList/Archive capture a one-shot entry snapshot before navigation, TDLib `"0"` remains a valid first-incoming boundary, and both initial and late around-unread restore target the semantic marker instead of the preceding date key that can drift during same-day prepends. The marker is a full-width `25vp` bar with `13fp` regular centered copy and `6vp / 5vp` vertical spacing. API23 opened live `HUAWEI Chat` with `5` unread messages directly on `Не прочитано`; bar `[0,1366][1308,1454]` and text `[503,1382][805,1438]` share center `(654,1410)`. Evidence: `.codex/ui-audit/2026-07-17/unread-marker-density/12-final.jpeg` + `.json` and `runtime-metrics.json`. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; the final clean HAP was installed and launched as fresh PID `7207` after an observed no-process interval, SHA-256 `D27197356849BE09FF7BC445A7723E936A4B517544B06FCB50D6FB7B1FB8ED3B`.
- **Chat date separators now match Telegram iOS in both density and label semantics:** the date variant uses the source-defined `22vp` pill inside the existing `34vp` timeline lane (`13fp`, `6vp` horizontal inset, `11vp` radius), while multi-line service events keep independent `34vp` geometry. Recent non-today dates no longer collapse to weekday abbreviations; app-language resources now render full localized month/day and add the year only across calendar years. API23 changed each date pill `119px -> 77px` and its `ListItem` `161px -> 119px`; live labels changed `10 июл / Сб / Вт -> 10 июля / 11 июля / 14 июля`. Evidence: `.codex/ui-audit/2026-07-17/date-separator-density/04-final.jpeg` + `.json` and `runtime-metrics.json`. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; installed HAP SHA-256 `9E7A132B105D41AA4AABFC5DC28CA27109F3FE476B810C2F0FB27DCE85EF9801`.
- **Outgoing interactive-file UI now follows Telegram iOS's complete palette, lane geometry and density:** duration/audio/document copy and inactive waveform use outgoing secondary; controls use outgoing primary with a bubble-color cut-out glyph. Voice duration sits below an `18vp` waveform, widths follow the `2...30s` interpolation, the content lane has no extra vertical padding, and status uses iOS's effective `5vp` overlap instead of a full row. API23 keeps widths `462px / 616px` and `44vp` controls, while both outer voice heights shrink `301px -> 199px`; duration/time glyphs retain a positive `5px` gap. Earlier pixel witnesses prove the qualified palette path. Evidence: `.codex/ui-audit/2026-07-17/voice-file-outgoing-secondary/`, `voice-waveform-outgoing-secondary/`, `voice-control-direction-palette/`, `voice-waveform-duration-lane/` and `voice-compact-density/`. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; installed HAP SHA-256 `7BE13DAA095FC8E81C923736CE761289A82840F5263ED6CFE0D206E2BF32DB1B`.
- **Standalone sticker status now follows Telegram iOS `FreeIncoming` / `FreeOutgoing` instead of the stronger image-media overlay:** the shared time/check geometry is unchanged, but sticker pills use a dedicated black 20% service-date fill while photo/location media retain black 40%. On API23 the live `11:16` sticker node kept identical text `[1071,707][1168,752]` and pill `[1050,688][1245,771]` bounds while the rendered background changed exactly `#66000000 -> #33000000`, removing the heavy black blob visible against the supplied iOS reference. Evidence: `.codex/ui-audit/2026-07-17/sticker-free-meta/02-after.jpeg` + `.json` and `runtime-metrics.json`. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `2679A003FF46E2637D49113BAE46C9B187EDDA3AF996704DFB0C0A7569E76880`.
- **Outgoing time/edited status and delivery checks now use the iOS bubble-aware secondary semantic instead of generic grey or saturated Telegram blue:** `MSG_META_TEXT_OUTGOING`, `MSG_META_STATUS_SENT` and `MSG_META_STATUS_READ` share the qualified `message_meta_outgoing` resource, while incoming/pending/error states and global `text_secondary` remain unchanged. The API23 live `изменено 07:42` ROI moved from grey `far50 mean RGB 140.7/151.1/166.3` to pale-blue `149.0/186.6/223.1`; pixels near the new semantic changed `0 -> 1460`, and pixels near the old secondary changed `749 -> 0`. Evidence: `.codex/ui-audit/2026-07-17/message-meta-outgoing-color/02-chat.jpeg` + `.json` and `runtime-metrics.json`. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `1DAB95063C6ADBEB81614BB3955355FB5FC2C56FB9FB500515BAFF430ECC182F`.
- **Edited messages now expose Telegram's localized status instead of silently showing only the time:** full TDLib messages preserve `edit_date`, live `updateMessageEdited` updates the same state through `MessageUpdatedEvent`, and `ChatTimelineVO` projects the localized label into one measured/rendered meta string. On API23 the real searched message now renders `изменено 07:42` at `[888,1041][1172,1086]` inside the outgoing bubble `[570,955][1266,1116]`; on this narrower viewport the two-pass layout correctly moves the enlarged meta to its own row rather than clipping it. Evidence: `.codex/ui-audit/2026-07-17/message-edited-meta/09-final.jpeg` + `.json` and `runtime-metrics.json`, fresh PID `30628` after an observed no-process interval. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `7CC49AC4E69BCB184A3111ABEC43E7A4F7D9A1E9C39C78612B1BFEA7DB6EC941`.
- **Compact text bubbles now use Telegram iOS's real `0.85` maximum fill instead of the contradictory `0.75` cap:** intrinsic shrink-wrap remains authoritative, but the wider ceiling lets text + time/status stay on the final line when iOS can keep them inline. On the same live `edit-guard-test-plus-edit` message, the outgoing bubble changed from `[570,925][1266,1086]` (`696×161px`, separate meta row) to `[346,1044][1266,1142]` (`920×98px`, inline `07:42` + read status), closely matching the supplied iPhone geometry without changing regular-width `0.65`, channels, native HDS chrome or message behavior. Evidence: `.codex/ui-audit/2026-07-17/text-bubble-compact-fill-085/01-bottom.jpeg` + `.json` and `runtime-metrics.json`, fresh PID `15023` after an observed no-process interval. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `6003669F9A778B568F1C062EBA8FA40A4E9769A095AD718AFAF033B98EC7097A`.
- **Chat top-bar action glyphs now match the supplied shipped iOS glass controls:** Back, Search and search-mode Close use a dedicated qualified theme-foreground token instead of the global Telegram-blue action color, while online/activity accents stay blue and API26/API23 material ownership is unchanged. On API23 the same Back/Search ROIs moved from `323/409` blue-like pixels and `0/0` white-like pixels to `0/0` blue-like and `510/620` white-like pixels. Evidence: `.codex/ui-audit/2026-07-17/chat-topbar-action-foreground/01-after.jpeg` + `.json` and `runtime-metrics.json`, fresh PID `8062` after an observed no-process interval. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `7B03688D4987442E2E752A687F8D2638974202C545925D74A5342EEDF0526D7C`.
- **Dark chat bubbles now use the current shipped iOS visual hierarchy instead of the older low-contrast palette:** dominant solid surfaces measured from the supplied iPhone 16 Pro Max screenshot are applied only through qualified semantic resources (`incoming #1E2E3D`, `outgoing #406D97`), leaving the light theme, native HDS bars and message atom logic unchanged. API23 `uitest` proves that the live `zai` text bubbles consumed the intended path: incoming `Stack [42,2220][896,2654]` renders `#FF1E2E3D`, outgoing `Stack [570,925][1266,1086]` renders `#FF406D97`. Evidence: `.codex/ui-audit/2026-07-17/dark-bubble-ios-colors/01-after.jpeg` + `.json`, fresh PID `31399` after an observed no-process interval. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `2C09EF5F9636104641D73580D2C5EB9C7DF9DE199017E41CC6F3525553323250`.
- **ChatList row density now matches the current Telegram iOS base-font geometry:** the shared `CHAT_ROW_HEIGHT` moved from `76vp` to `72vp` while the iOS-sized `60vp` avatar, typography, per-line meta lanes and separators remain unchanged. API23 `ListItem` height moved from `266px` to `252px` (`76vp -> 72vp` at 3.5x); the first-row avatar is `[35,654][245,864]` inside `[0,633][1308,885]`, proving the intended `6vp` top/bottom inset with no clipping. The denser list exposes more content and visually matches the supplied iPhone reference. Evidence: `.codex/ui-audit/2026-07-17/chatrow-density-72/01-after.jpeg` + `.json`, fresh PID `30378` after an observed no-process interval. Both shell smokes, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `FAFFA289B959C94352AD7C3A1C824C098EA822FA0904C63D20DD243B2990AB0F`.
- **ChatList rows no longer waste one shared 76vp meta lane on both text lines:** `TgChatRow` now follows current Telegram iOS source geometry with independent intrinsic `TgChatMetaTop` (`date/status`) and `TgChatMetaBottom` (`badge/mention/pin`) trailing lanes. A time label no longer truncates the preview, an unread badge no longer truncates the title, and a row without a bottom accessory lets its preview reach the trailing inset; vertical line heights and the composed legacy/demo wrapper remain stable. API23 live `zai` proves the intended branch: title row keeps its trailing time while the no-accessory preview expands from `x=273` to the exact content edge `x=1273`; `File` independently reserves status/time above and pin below. Evidence: `.codex/ui-audit/2026-07-17/chatrow-line-meta/02-after.jpeg` + `.json`, fresh PID `18961`, STIME `18:24:53`. PowerShell+Bash shell smoke, clean main `assembleHap` and `OhosTestCompileArkTS` are green; main HAP SHA-256 `FC46705E4D6D04E5D04BCFF4A95C6CE88F3DC17D80EA86337A1304AA1AB03C91`.
- **ChatList title avatars now come from real TDLib active stories instead of empty/fake preview inputs:** `loadActiveStories(storyListMain)` feeds a typed `updateChatActiveStories` normalizer/event/reducer path, preserves the exact `(order, chat_id)` descending order, hydrates missing poster chats through `getChat`, and derives up to three avatars from the existing `ChatItemVO` identity path. Read/unread/close-friends rings use qualified resources; the strip and Add Story action remain non-interactive until real viewer/posting controllers exist. The positive API23 witness is now captured: `loadActiveStories` was sent at `18:41:03`, and the title rendered three live ordered avatar previews at `[466,175][550,259]`, `[536,175][620,259]`, `[606,175][690,259]` with green close-friends rings before `Чаты`; evidence is shared with `.codex/ui-audit/2026-07-17/chatrow-density-72/01-after.*`. Focused reducer/normalizer/serializer/use-case coverage, shell smoke and clean main build are green.

## Latest (2026-07-16)

- **Archive unread count now uses the iOS inactive badge instead of active Telegram blue:** `TgArchivedChatsRow` passes the archive group-reference state through `TgChatMeta(isMuted: true)`, matching Telegram iOS `GroupReferenceData` (`unreadCount = (..., true, ...)`) without duplicating badge geometry. The same live count `15` changed from dominant RGB `50/143/236` to `183/182/188`; evidence `.codex/ui-audit/2026-07-16/archive-muted-badge/01-after.jpeg` + dump/metrics, baseline `.codex/ui-audit/2026-07-16/chatrow-title-icons/02-after.jpeg`. Shell smoke and clean `assembleHap` pass; HAP SHA-256 `7CF1C15B1EEE262764BABBB286BBA1C712EB5C5C287D3DE4FC1DAFC9EB1E7E57`, fresh PID `28178`, STIME `03:34:25`.
- **ChatList title decorations now follow the rendered title instead of drifting to the right meta edge:** the title `Text` uses ArkUI `flexShrink(1)` rather than `layoutWeight(1)`, matching Telegram iOS `titleLayout.trailingLineWidth` placement while preserving ellipsis and the fixed `TgChatMeta` lane. API23 live `💬 HUAWEI Chat` measured title `[273,1502][715,1568]`, mute `[729,1514][771,1556]` (14px gap), and time meta starting at `x=1149`; screenshot/dump/metrics are under `.codex/ui-audit/2026-07-16/chatrow-title-icons/`. Shell smoke and clean `assembleHap` pass; HAP SHA-256 `BBB92B4D367C7428508F03AAEE21726B2C0148494EDDEC930ED63BE947D0F1DD`, fresh PID `24619`, STIME `03:27:21`.

## Latest (2026-07-15)

- **Reply-сниппеты больше не застревают на `Reply / Сообщение`, когда оригинал вне loaded history:** typed `getRepliedMessage` path ограничен 8 запросами за pass / 32 за page session, originals лежат в context-scoped associated storage по relation key, а обычная история не получает cross-chat записи. Live ArkGram показал важный TDLib-контракт: relation chat `-1003321986781`, но returned original `chat_id=-1003490708234`; origin и relation теперь не смешиваются. Дополнительно закрыты два молчаливых integration gap: `replyMessageHydrated` добавлен в root reducer, а compact reply stamp меняет `LazyForEach` key для замороженных `@ComponentV2 @Param`. API23 delta: `Reply / Сообщение` `3 → 0`, реальный сниппет `ArkGram / ArkGram-v1.0.2.hap`; evidence `.codex/ui-audit/2026-07-15/reply-hydration-final/17-rootroute-witness.jpeg` + hilog. Shell smoke, `OhosTestCompileArkTS` и clean main build зелёные; HAP SHA-256 `1D50AAD2FD2F24F50EB9E8E5361B126792C89CD0C06C0086A1E5A5E9B6F4DB4E`, fresh PID `17423`, STIME `03:13:23`.
- **ChatList previews больше не смешивают русскую оболочку с английскими/сырыми TDLib labels:** `ChatItemVO` теперь сначала сводит content/bracket/service/typing states к стабильным `ChatPreviewKind`, затем разрешает media/service/activity text через app resources. Известные `[Sticker]`, `[ChatAddMembers]`, `[PinMessage]` и остальные placeholders не доходят до `TgChatRow`; base/ru_RU/zh_CN покрывают album/audio, service и volatile action labels. API23 live `Fly Emirates` изменился с английского `Photo` на `📷 Фото`, visible dump содержит `RAW_BRACKET=0`, при неизменных native HDS bars: `.codex/ui-audit/2026-07-15/chatlist-preview-localization/04-final-clean.jpeg` и `runtime-metrics.json`. Focused `ChatPreviewText` tests compile, shell smoke и clean main build зелёные; HAP SHA-256 `5A13721178A2A19271D059052754A7071D5EEDBD90046B1CDFECE7FB495AA8A5`, fresh PID `6800`.
- **Peer Info теперь открывает живой список участников вместо тупиковой disclosure-строки:** typed use-case выбирает pageable `getSupergroupMembers` для обычного списка и `searchChatMembers` для поиска/basic group, сохраняя `messageSenderUser`/`messageSenderChat` и роли creator/admin/bot. Custom `TgProfileMemberRow` повторно использует live user/chat identity, presence и avatar state; тап открывает прямой user profile без выдуманного private chat. API23 `Dart & Flutter` показал 7007 участников, поиск `Nikita` вернул `43/43`, а scrolling загрузил `200/7007` и `300/7008`; evidence — `.codex/ui-audit/2026-07-15/profile-members/15-members-visual.jpeg`, `18-members-search-nikita.jpeg`, `20-member-profile.jpeg`, `22-members-paged-hilog.txt`. `ohosTest` ArkTS compile, shell smoke и clean main build прошли; HAP SHA-256 `CE02E4C5E5365EF9DA9E913B15231A7BE03720518C8DED454541D1C7AAA94E78`.
- **Shared Media rows now execute the same content actions as chat bubbles instead of stopping at the source message:** a typed one-shot `PendingSharedMediaActionSignal` unwinds Profile back to the chat and dispatches photo/video to the existing gallery, voice to `MediaPlaybackController`, files to the existing document path, and exact extracted URLs to `UIAbilityContext.openLink`. Documents first probe native Preview Kit (`filePreview.canPreview` / `openPreview`) and fall back to the existing system `viewData` route; media-caption URLs are now retained before media-kind classification. API23 witnesses show gallery `11 / 11`, active voice Pause state, browser launch, and the document Preview Kit probe followed by the platform's unsupported-`.md` chooser: `.codex/ui-audit/2026-07-15/shared-media-actions/`. Focused tests compile, shell smoke and clean main build pass; final HAP SHA-256 `9791FB1C1AC8D0A101BD1002528EC965BF82AA47235917BE7D91153E7C9FA033`, fresh PID `26652`.
- **Shared Media thumbnail pipeline больше не блокирует свежие ячейки за старой synchronous-очередью:** TDLib file ids теперь читаются через `getTopLevelNumber('id')`, поэтому вложенный строковый `remote.id` больше не превращает валидный id в `0`. `TgSharedMediaPage` запрашивает только первый 21-cell viewport и диапазоны, реально проходящие через `Grid.onScrollIndex`, с максимум двумя активными request workers; `downloadFile(synchronous=false)` завершается через `updateFile` / `FilesState`, а ошибки логируются с file id. API23 `File → Profile → Media` показал delta placeholder-grid → заполненный viewport за ~3 секунды, затем тот же delta после быстрого scroll; evidence — `.codex/ui-audit/2026-07-15/shared-media-priority/20-final21-open-1s.jpeg`, `21-final21-open-4s.jpeg`, `22-final21-scroll-350ms.jpeg`, `23-final21-scroll-3s.jpeg`, `final21-hilog-scroll.txt`. Focused test coverage добавлен; `ohosTest` ArkTS compile, shell smoke и clean main build зелёные (on-device test HAP не ставился, чтобы не стереть live session).
- **Telegram emoji-панель теперь содержит полный neutral/base Unicode 17.0 каталог, а не короткие sample-массивы:** generated `TgUnicodeEmojiCatalog` фиксирует 1914 fully-qualified emoji в девяти CLDR keyboard groups (Smileys → Flags), без размножения skin-tone variants в основной сетке. `TgComposerEmojiPanel` добавил отдельные People/Flags, localized section titles и `LazyForEach`/`IDataSource`, поэтому сотни offscreen cells не создаются eagerly. API23 runtime в реальном `zai` подтвердил 10 rail tabs с Recent, `Люди и жесты`, `Флаги` и изменившийся набор видимых flags после scroll; evidence — `.codex/ui-audit/2026-07-15/emoji-catalog/03-people.jpeg`, `04-flags.jpeg`, `05-flags-scrolled.jpeg`, `runtime-metrics.json`. Focused contract, resource JSON, deterministic regeneration, shell smoke, clean main build и ohosTest ArkTS compile зелёные; HAP SHA-256 `F2B66F4D2E75863F5435962558A12B8FF5D929E23425071BEC02E4ABBA5FDB39`, fresh PID `25099`, STIME `22:07:19`.
- **Chat root теперь адаптивный master-detail shell на native `NavigationMode.Auto`:** `HdsTabs` и отдельный Archive stack остаются в master, chat/profile открываются в detail, а пустой detail повторяет iOS wallpaper + «Начните общение». Master следует Telegram iOS формуле `min(max(320vp, width/3), width/2)` и отделён native 1px divider. API23 runtime: landscape master `[0,0][1120,1308]`, detail `[1121,0][2880,1308]`, HDS island `[56,1035][1064,1203]`; portrait round-trip восстановил те же title/Search/first-row/tab bounds, что initial (`[586,184][722,250]`, `[70,318][1238,458]`, `[0,633][1308,899]`, `[80,2607][1228,2775]`). Safe-area zero после rotation закрыт orientation-aware portrait cache + re-alignment первого row только когда список стоит на start. Evidence: `.codex/ui-audit/2026-07-15/chat-split-navigation/29-final-portrait-initial.jpeg`, `30-final-landscape-empty.jpeg`, `33-final-landscape-chat.jpeg`, `35-final-portrait-roundtrip.jpeg`, `runtime-metrics.json`; HAP SHA-256 `1C05F76FEB838713B9748B62B3432A0E2EE96298181E7AC2897EBE1ED31D3721`, PID `10213`, STIME `21:31:34`.
- **Переход media bubble → fullscreen gallery теперь принадлежит только media surface, а не всему captioned bubble:** стабильный `sourceMessageId` связывает source/destination `geometryTransition`, `TgChatScreenPage` меняет gallery/source-hiding state в одном `animateTo` с native `curves.responsiveSpringMotion`, а `TgMediaBubbleShellV2` сохраняет layout скрытого source media по iOS `transitionNode`/`setupTemporaryHiddenMedia` contract. Runtime baseline `20-open-transition-contact-sheet.jpg` обнаружил неправильное масштабирование caption вместе с shell; после переноса host на media node открытие `52-open-media-only-contact-sheet.jpg` и закрытие `54-close-media-only-contact-sheet.jpg` показывают отдельное расширение/сжатие изображения при неподвижном gallery caption и восстановлении source. Focused contract, shell smoke, clean main build и ohosTest ArkTS compile проходят; API23 PID `13907`, STIME `19:10:38`, main HAP SHA-256 `E32ADBA2A1123E55E4576B442DC496FB0174C0662FE2208E1A6F8FC16D0D0148`.
- **Корневой badge вкладки Chats теперь показывает реальный агрегат TDLib и остаётся внутри native HDS tab item:** `updateUnreadMessageCount(chat_list, unread_count, unread_unmuted_count)` проходит через normalizer/event/reducer в `ChatsState`, а selector использует точный main-list aggregate с fallback только по загруженным main chats. Общий formatter чисто повторяет iOS `compactNumericCountString` (`1K`, `2.5K`, `1.2M`) и используется badge/meta/scroll FAB; прежние число загруженных чатов и clamp `999+` удалены. `TgRootTabUnreadBadge` занимает только содержимое `bleedIconStyle`, поэтому floating island, material и motion по-прежнему принадлежат `HdsTabs`. API23 runtime показал live badge `4.6K` в bounds `[773,2590][900,2653]`; screenshot `.codex/ui-audit/2026-07-15/root-tab-unread-badge/01-after-runtime.jpeg`, fresh PID `16207`, STIME `18:06:57`. Focused source/host contract, оба shell smoke, clean main build и финальный ohosTest ArkTS compile green; main HAP SHA-256 `951ED89A2D1CA56A30C869CA965D535D0C07FCBFD95BBB106B698EA36E4F085B`.
- **Text-message link previews now have a complete Telegram custom-content path instead of being dropped at DTO parsing:** the current `messageText.link_preview` contract (with legacy `web_page` fallback) carries site/title/description/order/media flags and poster ids through reducer state, immutable clones, background thumbnail download, `ChatTimelineVO`, recycled-row invalidation and `TgMessageRouter`. New token-first `TgLinkPreviewBubble` ports the current iOS accent-rail hierarchy with text-only, compact trailing-media and large-media states; preview rows establish a readable minimum width and force meta below the card. Passport/demo and focused parser/reducer/file/download/timeline coverage were added. The source contract was observed red then passes; PowerShell+Bash shell smokes, clean main `assembleHap` and ohosTest ArkTS compile pass. Main HAP SHA-256 `9438C1256E069D2803A63BE07CDCF8131F93DE75BAE8113220874A0D7B4DE500`. Runtime visual acceptance remains pending because this slice did not take over the shared emulator.
- **Chat top-bar capsules now protect their content over a moving timeline on API23 without restoring the rejected full-width panel:** only `TgChatTopBar` uses a dedicated qualified translucent veil (`#BFFFFFFF` light, `#B32C2C2E` dark) above the existing adaptive blur; ChatList/Profile/Forward and API26 `ImmersiveMaterial` remain untouched. In the real `File` chat, the dark title-island sample moved from the bubble-derived RGB `67/98/128` to a neutral `54/67/83`, while light moved from green-derived `233/250/210` to neutral `252/253/250`; capsule geometry and the exposed wallpaper gaps are unchanged. Evidence: `.codex/ui-audit/2026-07-15/chat-topbar-content-protection/01-file-dark-after.jpeg`, `02-file-light-after.jpeg` and `runtime-metrics.json`. Focused contract, both shell smokes and clean `assembleHap` pass; HAP SHA-256 `426DB0BA2D772DC7EB3E275E786313B7DF1EC46A0896CF69E2B246BB67088D5A`, fresh API23 PID `6326`, STIME `16:37:17`.
- **ChatList Search is now a finished native-input path on API23:** the centered idle placeholder explicitly hands focus to the stable ArkUI `Search`, which raises the system IME and filters the real chat data (`File` + `ArkGram`). The root keeps `HdsTabs` native, but consumes the documented `TYPE_KEYBOARD` avoid area and applies its existing zero-height/zero-opacity tuple while the IME is visible, so the floating island no longer rides above the keyboard; closing the IME restores the exact `[80,2607][1228,2775]` tab bounds. A fully collapsed invisible Search switches to `HitTestMode.None`; runtime tap on `Новые` moved the selected capsule background from `Все` to `Новые` and replaced the visible data set. Evidence: `.codex/ui-audit/2026-07-15/chatlist-search-focus/10-keyboard-driven-hidden.jpeg`, `11-file-filter-no-tabs.jpeg`, `12-keyboard-closed-tabs-restored.jpeg`, `17-collapsed-unread-filter.jpeg`, `19-final-clean-settled.jpeg` and `runtime-metrics.json`. Focused geometry/search contracts, both shell smokes and clean `assembleHap` pass; final HAP SHA-256 `31955A90CF2CCCE0C0C8AF499C391017E5F0F4D74BED8F02507244624E1C0034`, fresh API23 process PID `23879`, STIME `16:07:35`.
- **The real Forward target picker now has finished modal glass chrome instead of the legacy full-width `TgTopBar`:** a dedicated `TgForwardPickerNavigationBar` keeps Telegram iOS `PeerSelectionController` hierarchy (modal title + native ArkUI `Search` + real `TgChatRow` content), localizes the Russian title to `Переслать`, and uses independent 38vp Back / 128×40vp title capsules with 44vp Back response geometry. API26 uses `ImmersiveMaterial`; API23 uses the adaptive blur fallback and measures the real status-bar avoid area. Runtime bounds are Back `[56,154][189,287]`, title `[430,150][878,290]`, Search `[56,329][1252,455]`; the list still starts at `y=479` and rows remain `266px`, so the chrome polish introduces no content jump. Query `File` returned real `File`/`ArkGram` rows; Back returned to `Живи футболом` with no forward snippet or send. Evidence: `.codex/ui-audit/2026-07-15/forward-picker-polish/09-final-picker.jpeg`, `10-search-file.jpeg`, `11-back-channel.jpeg` and `runtime-metrics.json`. Focused/share contracts, both shell smokes and clean `assembleHap` pass; HAP SHA-256 `F3183D092C8838B1D9A00388376A20A8456700CFA3680A0C5AD57671323539C8`, fresh API23 process PID `22628`, STIME `15:02:00`.
- **Broadcast-channel posts now use the Telegram iOS full-width lane instead of the generic 75% cap:** `TgChatScreenPage` derives `Chat.type === 'channel'`, `TgMessageRouter` switches only that state to the iOS `allowFullWidth` policy, and incoming posts retain the source-defined `45vp` trailing share-action lane. API23 real-channel bounds moved from `[42,1386][956,2654]` inside a 1224px lane (74.67%) to `[42,1522][1092,2654]` (85.78%); the right gap fell from 310px to 174px. The real `Rozetked Plus Chat` group remained on its existing avatar-aware width path (`08-group-control.jpeg`). Evidence: `.codex/ui-audit/2026-07-15/channel-bubble-width/04-before-channel.jpeg`, `07-after-candidate.jpeg`, `08-group-control.jpeg` and `channel-width-metrics.json`. Focused contract, shell smoke and clean `assembleHap` pass; HAP SHA-256 `E5D4BD1AE6F7DA29900168BDF43CB34C04B1D190992FD09E92EA3F1665462423`, fresh process PID `10785`, STIME `13:34:12`.
- **The reserved broadcast lane now contains the real Telegram iOS share action:** incoming channel posts render a 30vp circular glass `TgChannelShareButton` at the exact 8vp bubble gap and 1vp bottom inset, with a 40vp `responseRegion`; API26 uses `ImmersiveMaterial(THIN)` and API23 keeps the adaptive blur fallback. The action reuses `ChatMessageActionsController` and the existing typed `forwardMessages` picker path rather than introducing another send flow. API23 measured the two visible buttons at `[1120,1244][1225,1349]` and `[1120,2546][1225,2651]`; the bubble edge is `x=1092`, giving the intended `28px / 3.5 = 8vp` gap. Tapping the first button opened the real `Forward` target picker; no target was selected and nothing was sent. `Huawei HarmonyOS` group control reports zero share buttons. Evidence: `.codex/ui-audit/2026-07-15/channel-share-button/11-final-channel.jpeg`, `12-forward-picker.jpeg`, `14-group-control.jpeg` and `channel-share-metrics.json`. Focused contracts, shell smoke and clean `assembleHap` pass; HAP SHA-256 `BD60EC33E41549A79924894BEF5ADA0C983490FAA7109F3939763D5B6D3D9881`, fresh API23 process PID `32672`, STIME `14:18:06`.
- **Poll bubbles no longer escape narrow group/avatar lanes:** the fixed unbounded `240vp` body minimum and duplicated router+atom padding were replaced with the Telegram iOS rule `min(280vp, available content width)` and single inset ownership. Option radio controls start at `12vp`; option text, result bars and separators share the iOS `50vp` axis; result percentages lead the answer text; type/footer labels are localized for base/Russian/Chinese resources with Russian few/many vote grammar. API23 synthetic integration through the real `TgMessageRouter` measured the incoming bubble at `[227,466][966,1392]` inside its `[94,466][1214,1392]` message lane and the outgoing result bubble at `[374,1557][1214,2454]`. Evidence: `.codex/ui-audit/2026-07-15/poll-bounded-layout/02-localized-final.jpeg` and `.json`; witness HAP `AA4ED66C...C7171`. The temporary witness entrypoint was removed, clean production `assembleHap` passed, production HAP SHA-256 is `CC34F391AE930E9EE7460A4065B1CD0166A3B4C3AE5CB70C43F547596E9DA519`, and the preserved session returned to ChatList in fresh process PID `32100`, STIME `13:12:49` (`03-production-return.jpeg`). A real TDLib poll dataset is still needed for tap/vote behavior acceptance; no poll was sent or fabricated in the user's account.
- **ChatList chrome and surfaces now match the shipped iOS dark hierarchy:** Edit/Add Story/Compose keep the accepted in-chat `38vp / 19vp / 20vp / 44vp response` contract, Search remains `40vp`, and the filter rail remains proportionally smaller at `32vp`. Glass action foreground is primary white, Archive text is pinned to the normal row axis, and a ChatList-scoped navy palette now separates header/pinned (`#203040`), Search (`#344454`) and regular rows (`#18212D`) without changing other screens. The old 8vp pinned-group gap is a separator, and the opaque header surface shrinks with the exact 54vp Search collapse so it never covers rows. API23 witnesses: `.codex/ui-audit/2026-07-14/chatlist-navy-surface/03-expanded-final.jpeg` and `04-collapsed-final.jpeg`. HAP SHA-256 `8EDCAB459AD0598ABEF955042DA36DA05D831ACCEC666893F4978F1C5684D339`, fresh process `STIME 11:50:55`; real filters/native HDS tabs are unchanged and Story inputs remain intentionally empty until real TDLib Story data exists.
- **Peer Info navigation is now a finished glass atom instead of two naked icons:** `TgProfileNavigationBar` ports Telegram iOS's independent left/right `GlassContextExtractableContainer` boundary, exact 16pt side inset and 44pt interaction contract while retaining the accepted compact 38vp visuals. API26 uses interactive `ImmersiveMaterial(THIN)`; API23 uses adaptive ultra-thin blur, tint and edge without a full-width plate. The atom has a passport/demo and localized accessibility labels. API23 `File` profile shows separate circular Back/Search surfaces at `[56,164][189,297]` and `[1119,164][1252,297]`; Back returned to `File`, and Search returned with `tg_chat_search_input focused=true`. Evidence: `.codex/ui-audit/2026-07-13/profile-navigation-glass/03-file-profile-after.jpeg`, `04-back-to-file.json`, `05-search-to-file-focused.jpeg`. Runtime HAP SHA-256 `FEB68566BEBA80E6649120BC1582FC5B2C1DDEC13842B0A24D36E4BB400A4F5D`; focused contract and clean main build are green.
- **In-chat search polish is complete and runtime-verified past the first TDLib page:** search now preserves the exact `foundChatMessages.next_from_message_id`, prefetches the next 100-result page near the loaded edge, deduplicates ids and rejects stale query responses. The composer replacement is two independent 40vp glass capsules with native `LoadingProgress`, localized pending/empty/error states and system-tinted chevrons; opening Search focuses the conditional `TextInput` on the next frame and raises the system IME. Group/channel bars retain the iOS avatar/info action and expose Search from the profile header. API23 `Rozetked Plus Chat` query `iphone` logged `loaded 100 more results (200/2997)` and reached `102 / 2997`; direct `zai` and profile -> `File` dumps both report `tg_chat_search_input focused=true`. Evidence: `.codex/ui-audit/2026-07-13/chat-search-polish/27-zai-search-focus.jpeg`, `32-file-search-from-profile.jpeg`, `40-rozetked-search-beyond100.jpeg`. Runtime HAP SHA-256 `38FE16A88FBFFEA5FD394D53397F9070F64690F023D53E6DA9F50E2F2E95DAC2`; focused contract, resource parse, shell smoke, main build and ohosTest ArkTS compile are green.
- **The chat scroll-to-bottom control now has the real Telegram unread state:** a reusable custom `TgScrollToBottomButton` ports iOS's 40vp glass control and centered 20vp accent badge (`13fp`, `y=-7vp`, compact `K/M` values). The page retains the live chat unread count before `markChatRead` can reset TDLib state, only raises it on later updates, and clears it at the true bottom or on press without changing the existing mid-history latest-message jump. API23 opened `Москвач • Новости Москвы` at its unread boundary with a centered `62` badge, then the same button jumped to the current bottom and disappeared; witnesses: `.codex/ui-audit/2026-07-13/scroll-fab-unread/05-moscow-unread-entry.jpeg` and `06-after-jump-bottom.jpeg`. Runtime HAP SHA-256 `8CA756707D70AF9D195C303129069B5ED5019B199243CEC3BE98AE8F0741EBD5`; focused contract and ArkTS/HAP build are green.
- **Settings is now a finished native/custom root surface:** the HDS title is pinned in safe `FULL` mode after API23 proved that collapsing `FREE` could enter the status bar, while the custom Telegram groups now match the current iOS order: Saved Messages / Recent Calls / Devices / Chat Folders, the six advanced rows including Power Saving and localized `Русский`, plus Support / FAQ / Telegram Tips. The live profile remains account-backed, the bottom group clears the floating HDS tab island, and Recent Calls switched to the real Calls tab (`13-recent-calls-route.json`). Final visual witnesses: `.codex/ui-audit/2026-07-13/settings/12-final-top.jpeg` and `14-final-tips-bottom.jpeg`. Runtime HAP SHA-256 `53397FE6FE390B14580DBCBD974C2CF93C375D1E35F99FB1340F172917A26B52`; focused contract, resource parse and ArkTS/HAP build are green.
- **Contacts is now a finished native/custom Telegram surface:** API23 renders the live contact set through native ArkUI `Search`, sticky `ListItemGroup` sections and `AlphabetIndexer`, with Cyrillic-first Russian grouping, emoji-safe initials, locale normalization and stable programmatic index selection. The list viewport itself owns the HDS-title inset, so Search/headers/rows no longer slide under the title. Query `Рашид` filtered to the real `🇰🇿Рашид Билайн`; witnesses: `.codex/ui-audit/2026-07-13/contacts/23-final-loaded.jpeg` and `24-final-search.jpeg`. Runtime-tested HAP SHA-256 `C097522BC9F04AB6D9C5F6209ED613501B321656879D2EAB032D6985ACE51127`; focused contract and clean build are green.
- **Calls tab is now a finished native/custom Telegram surface:** the API18+ native `CapsuleSegmentButtonV2` lives in the HDS title-bar `bottomBuilder`, while the custom list consumes real typed `searchCallMessages` data for All/Missed. The runtime no longer overlaps the first row, direction copy uses Telegram's compact one-line Incoming/Outgoing/Missed labels, dates render `d MMM` / `d MMM yyyy`, and the trailing control is the iOS info glyph. Russian root-tab/title resources are active. API23 switching from All to Missed removed the outgoing row and issued a fresh `searchCallMessages` transaction (`Loaded 2` -> `Loaded 1`); witnesses: `.codex/ui-audit/2026-07-13/calls/04-all-final.jpeg` and `05-missed-final.jpeg`. Runtime-tested HAP SHA-256 `F1D73C0A5C9B6FC778AB9EF2D543D1B050AA4BE0B6E00715BFA44FADECA872E9`; focused contract and clean build are green.
- **Archive chat list is now a real Telegram surface, not a placeholder:** `ChatsState` independently tracks `chatListMain` and `chatListArchive` positions, the main list renders an iOS-style archive group with a dialog-count unread badge and real peer preview, and `TgArchivedChatsPage` owns the real archived rows, search, bounded pagination and navigation. Main-list Archive and archive-list Pin/Unpin + Unarchive use typed TDLib `addChatToList` / `toggleChatIsPinned` commands. The reveal actions were visually corrected to the iOS icon-over-label hierarchy so Russian labels no longer clip. API23 runtime shows `11` unread archived dialogs and `98` real archived chats; final evidence: `.codex/ui-audit/2026-07-13/archive/11-final-main-icon.jpeg` and `13-final-actions-icon.jpeg`. No archive action was executed during the visual witness. Runtime-tested HAP SHA-256 `C41DAA3BA2D1F356EA016826432932594BD95A3A53CB3A0A078595D05AD89972`; focused contract and clean main build are green.
- **Real Telegram inline-bot GIF search is now wired and runtime-verified end-to-end:** GIF mode owns the iOS-style rounded Search rail and two-column cover grid rather than filtering saved animations or showing fake results. The typed path is `getOption(animation_search_bot_username)` -> `searchPublicChat` -> `getInlineQueryResults`, with fallback bot username `gif`, 250 ms debounce, exact string preservation for TDLib int64 query ids, stale-request guards, guarded `next_offset` pagination, and `sendInlineQueryResultMessage` on selection. TDLib `minithumbnail.data` now provides an immediate Base64 JPEG preview until a downloaded thumbnail arrives; no realtime blur filter is applied to the grid. On a clean API23 cold boot, one tap focused the Search field, query `cat` returned `50` inline results, and real previews rendered in the grid. The same PID (`STIME 16:21:27`) remained alive after the result view and a 35-second settle; no new Telegram AppFreeze or RenderService freeze file appeared. Evidence: `.codex/ui-audit/2026-07-13/gif-search/coldboot-final/final-gif-cat-system-ime.jpeg`. Runtime-tested HAP SHA-256 `FFAF0A36C907676271FF81627E9C29AB1F88A79C487A8FECDF3B6F12773888CA`; subsequent clean main build and ohosTest compile are green, final rebuilt HAP SHA-256 `394EA9EACB08C402AD42327F3F333BA2A6C4177F9841840FBABB2698139B02FD`.
- **Regular Telegram sticker search is now live in the iOS-style entity keyboard:** sticker mode orders the real pack strip, rounded Search rail, section title, four-column result grid, and shared `GIF / Stickers / Emoji` mode bar like the reference hierarchy. Queries from two characters use typed TDLib `searchStickers(stickerTypeRegular)`, current locale plus `en-US`, a 250 ms debounce, deduplication, and request/query/mode/panel/lifecycle guards; no local substring filter or fake GIF results are used. The new passport/demo are `tg_ui/spec/TgComposerStickerPanel.md` and `tg_ui/demos/TgComposerStickerPanelDemo.ets`. The focused contract was observed red before implementation and is now green; emoji/entity-keyboard contracts, shell smoke, clean main `assembleHap`, and ohosTest ArkTS compile are green. HAP SHA-256 `FB696F961A2957E28CA2BCB38AE427E8144E97BF95ED4BD96799EECA75A6AB6C`. Runtime visual/data acceptance remains pending while the lagging emulator is parked.
- **Emoji mode now exposes real Telegram featured/custom emoji and is runtime-verified on API23:** installed `stickerTypeCustomEmoji` packs remain first, while typed `getTrendingStickerSets(stickerTypeCustomEmoji)` supplies real featured packs when the account has none installed; ids are deduplicated and the synthetic Unicode-only strip collapses if both sources are empty. The fresh runtime returned `Installed custom emoji sets loaded: 0` plus `Trending custom emoji sets loaded: 12`; selecting the `Crayons Emoji` tab loaded a real `150`-item sticker set and rendered its eight-column grid. Selection still preserves exact int64 `textEntityTypeCustomEmoji` ids, and keyword search remains typed `searchEmojis` -> `searchStickers(stickerTypeCustomEmoji)`. Evidence: `.codex/ui-audit/2026-07-13/emoji-panel/featured-custom-emoji/02-emoji-panel.jpeg` and `05-custom-pack-real.jpeg`; HAP SHA-256 `B2D853FAB1D3FCD06AE6D1F72C70749DA48DB4B4DED8260D18359E43BE4A2558`, fresh process `STIME 17:23:16`. Focused contracts, shell smoke and clean main `assembleHap` are green.
- **Telegram entity-keyboard hierarchy plus entity-preserving custom-emoji editing are live on API23:** `GIF / Stickers / Emoji` remain peer modes in one bottom segmented control, while Favorites/Recent/installed/featured packs use an independent top strip. Recent owns the shipped iOS grouped home from typed `getPremiumStickers` / `getFavoriteStickers`, and bounded preview downloads replace fallbacks with real Telegram images. Custom emoji now insert at the active selection, retain/shift unaffected UTF-16 TDLib entities across manual edits, drop only intersected spans, and survive draft DTO/state restore. Focused device tests pass `TgCustomEmojiDraftPolicy 5/5` and `ChatDraftEntities 2/2`; runtime selection from `Crayons Emoji`, chat exit and re-entry restored the fallback text without sending or crashing. Final-build evidence: `.codex/ui-audit/2026-07-15/custom-emoji-rich-editing/11-final-settled.jpeg` and `12-final-draft.jpeg`; HAP SHA-256 `71E3F0E9942A17DC0BCE04FA842D12445A2DC0DCFC1E3452BAD273D39BB75DA1`, fresh process `STIME 12:12:58`. Plain `TextArea` still renders fallback glyphs; animated inline custom-emoji rendering remains a separate `RichEditor` migration.
- **Offscreen TGS repaint fixed; visible-animation cost remains open:** `hitrace` showed four cached TGS `Canvas` nodes repainting every app VSync (`260` Canvas render tasks and `65` app VSyncs in 3 seconds) even in the static `File` chat. `TgTgsPlayer` now participates in the documented `@ohos/lottie` visibility coordinator. Idle `File` chat fell from `4.13` to `0.46` host cores and its post-fix trace contains zero Canvas render tasks/app VSyncs. A visible TGS still keeps the software Lottie path expensive: the 30fps cap reduced Canvas draws to `77/3s`, but host load remained about `3.19` cores. Evidence: `.codex/ui-audit/2026-07-13/emulator-freeze/`. Do not call the whole emulator issue closed until the visible-animation policy is replaced or bounded.
- **Telegram emoji/sticker panel motion verified on API23:** both custom keyboard-slot panels now use the shared asymmetric `composerPopupTransition()` (opacity + vertical translate + scale) instead of appearing/disappearing immediately. The enter sequence and settled panel, the keyboard-glyph handoff back to the system IME, and the fresh process witness are under `.codex/ui-audit/2026-07-13/composer-panel-motion/`; settled bounds keep `List [0,137][1308,1617]` above `TextArea [496,1640][977,1724]`. HAP `4E24DEE0...4D4FCD`, fresh process `STIME 12:35:57`; focused contract, shell smoke and clean `assembleHap` are green.
- **Current iOS composer auxiliary modes verified on API23 (2026-07-15):** Telegram GIF/Stickers/Emoji remains a custom keyboard-slot panel mutually exclusive with the system IME. Field touch clears the custom modes before focus, and the keyboard glyph focuses `TextArea` plus calls IME `showTextInput()`. Attachments keep native ArkUI `bindSheet` presentation/transition, but no longer inherit the translucent composer plate or occupy the full screen: the Telegram content is an opaque navy 67%-height bottom sheet with a custom iOS-style grabber, compact Recent header, real permission-backed three-column PhotoAccessHelper grid and fixed Gallery/Camera/File/Location/Poll/Contact rail with selected Gallery state. API23 `dumpLayout` places the sheet title at `[482,1143][763,1213]` and category labels at `y=2733..2778`; the library is empty, so runtime truth remains the real `Пока пусто` state rather than fabricated thumbnails. Evidence: `.codex/ui-audit/2026-07-15/attachment-next/07-final.jpeg` plus `attachment_final.json`. Final HAP SHA-256 `09B6302D631C5DA359C43BCA8D50C48447214097BF8E805549CA56FE8E6F9968`, fresh process PID `17531`, STIME `12:44:25`; focused contract, shell smoke and `assembleHap` are green.
- **Composer now displaces the chat instead of covering it:** the timeline viewport physically ends at the measured composer top via content-layer bottom padding; `contentEndOffset` is no longer used as a substitute for viewport geometry. `TextArea` captures the bottom anchor on touch-down before IME `RESIZE`, and the page reapplies `ScrollAlign.END` as its height changes and as multiline input grows. API23 bounds prove idle `List y2=2654 / composer y1=2653`, focused `1597/1596`, and multiline typing `1170/1170`; in every state the last `ListItem` ends exactly at the List edge. Witnesses: `.codex/ui-audit/2026-07-12/composer-push/`. Exact final HAP `C32933DA...9C8776`, fresh process `STIME 17:20:27`; source contract, shell smoke and clean `assembleHap` are green. The test draft was cleared without sending.
- **Chat auxiliary panels moved to Telegram geometry:** pinned preview is now a 48vp rounded rectangle with 12vp corners instead of a pill. Bot commands are an in-flow full-width context panel above the composer. Emoji/stickers remain full-width 292vp keyboard replacements below the composer. Reference re-check corrected attachments: Telegram iOS `AttachmentController` and Android `ChatAttachAlert` are modal sheets, so Camera/Photo/Video/Document now use native ArkUI `bindSheet` (`SheetType.BOTTOM`, `SheetMode.OVERLAY`) with custom content instead of an in-flow pseudo-keyboard. API23 fresh-process open, Back-dismiss and reopen witnesses are under `.codex/ui-audit/2026-07-12/attachment-native-sheet/`; HAP `17DF873E...8815965`, process `STIME 15:29:57`. Source contract, shell smoke and clean `assembleHap` are green. A recent-media thumbnail grid remains a separate parity slice.

- **M4 pinned-message live slice verified on API23:** `TgPinnedMessagePanel` remains a custom isolated 48vp glass capsule, now driven by real typed `getChatPinnedMessage` data through `AppCommand`/serializer/`loadPinnedMessageInfo`. `TgChatScreenPage` owns request lifecycle, preview/thumbnail, timeline clearance, local close and jump. HUAWEI Chat runtime proves the localized pinned preview, tap-triggered `aroundMessage` load (`40 events`), centered media/album target and local X removal; witnesses are under `.codex/ui-audit/2026-07-11/pinned-message-live/`. Album rows now retain every source message id, fixing jump targets that point to a non-primary album item. HAP `33365406...2161BB`; source contract, PowerShell smoke, main and ohosTest ArkTS builds are green. Exact multi-pin count/list navigation remains a separate `searchMessagesFilterPinned` slice; no guessed count or fake `lastMessage` fallback is used.
- **Unified compact upper/lower controls verified on API23:** chat top-bar side controls remain `38vp`, title glass `40vp`, title/status typography `16/12fp`, and short-title floor `164vp`. The latest bottom-line correction reduces composer controls/capsule `36→34vp`, icons `22→21vp`, vertical inset `2→1vp`, and increases side insets `14→18vp`; popup rows remain `42vp` and primary touch regions remain 40vp (top bar 44vp). API23 bounds moved from `[49,2649][1259,2775]` to `[63,2660][1245,2779]`: 28px shorter and 7px thinner on the same 1308×2880 runtime. Fresh HAP `9EC32220...C582F`; idle/focused witnesses are under `.codex/ui-audit/2026-07-11/composer-thinner-shorter/`. Focused source contract, shell smoke and clean `assembleHap` are green.
- **C2 isolated adaptive iOS chat-title island:** the center top-bar capsule now wraps intrinsic title/status width instead of inheriting the full center slot. The compact pass keeps a 144vp content floor (164vp including the two 10vp insets); longer titles grow until the available side-control boundary and then ellipsize. The full-width `TgTopChromeBackground` panel was removed from chat detail, so wallpaper remains visible between/below the independent back/title/trailing capsules. API23 runtime captures confirm the short `zai` and `File` islands over exposed wallpaper.
- **C2 focus-driven composer motion:** private-bot Menu collapses as soon as the `TextArea` receives focus/caret, not after the first character. The compact 34vp Menu capsule and surrounding controls reflow with a 220ms smooth layout/label transition; the bot context panel keeps the custom 180/140ms transition while attachments use the platform sheet transition.
- **C2 compact composer + auxiliary panels verified on API23:** bottom controls/capsule are 34vp visuals (icons 21vp, 4vp gaps, 1vp vertical inset), and the shared idle/recording row uses 18vp side insets. `responseRegion` preserves at least 40×40vp touch targets. Camera/Photo/Video/Document remain routed into the existing picker controller; message long-press actions remain anchored glass popups. Every bot command remains inside the 4.7-row scrolling viewport with 42vp rows.
- **C2 iOS composer behavior + alignment verified on API23:** after the user rejected the feature-only pass, the runtime geometry was measured instead of inferred from source. The old last-bubble overlap (`y=2880` behind composer `y=2618`) is now closed by the physical viewport contract above, including focused IME and multiline states. The trailing control remains one persistent external slot: idle mic transforms in place into send rather than docking send inside the text capsule. Private-bot Menu uses three lines/X and focus/typing collapse while the input expands. Behavior still includes real hold/cancel/lock recording, TDLib voice send and CameraPicker action. API26 material runtime and hardware CameraPicker capture remain external gates; user visual acceptance is still required.
- **M2b Contacts `HdsNavigation` capability rejected and rolled back:** the native Search/same-`Scroller` path was exercised on target 26 / Pura API23, but `contentStartOffset`, in-list clearance, FREE and explicit MINI modes could not preserve a non-clipped title without overlap. The four scoped production/smoke files were restored byte-for-byte and the unaccepted passport removed; attempt evidence is under `.codex/ui-audit/2026-07-10/m2b-contacts-hds-navigation/after/`. The accepted M2a root shell remains the production baseline.
- **M2a native root-tab contract verified with zero visual delta:** `RootTabBarGeometry` now owns the unchanged visible/hidden HDS tuples and the preserved root-content inset policy; `MainTabsPage`, `SafeAreaUtils`, Chats, Contacts, Calls and Settings consume the shared contract. On the same Pura API23 emulator, light/dark before and after bounds are exactly `[80,2607][1228,2775]`, chat detail contains zero `TabBar` nodes and Back restores one. Fresh-process/session, all-tab switching and bottom-content witnesses are under `.codex/ui-audit/2026-07-10/m2a-native-tabs-contract/after/`. Floor `10` and fallback `82` remain intentionally unreconciled with render margin `30`.
- **M1 Chat Surface Foundation reverified on the current HAP:** `TgChatBackground` remains the lowest chat `Stack` layer with qualified light/dark clean-room PNG tiles and `COLOR_CHAT_WALLPAPER_BASE` as the solid fallback. API24 rendered dark base/bubbles as `#0F2837 / #1E2E3D / #406D97` and light as `#E9EFF2 / #FFFFFF / #E1FFC7`; the pattern stays visible but subordinate in both `zai` and `File`. A real timeline swipe changed the visible message window, composer focus resized the app for the system IME, and returning to ChatList preserved native HDS tabs. Evidence: `.codex/ui-audit/2026-07-19/m1-chat-surface-current-hap/`; installed HAP SHA-256 `17E2576E36BED2A0FAABD7E4BBFB2D0BCE525C437E4C4811D3D164996BA65EAD`, fresh PID `31547` (`STIME 22:28:24`). Focused source/resource assertions and `smoke-ui-phase0.ps1` pass; the emulator theme was restored to dark.
- **UI direction re-locked from live references:** user-provided iPhone 16 Pro Max captures, fresh Harmony light/dark witnesses and Nekogram 12.8.1 on MI 9 are compared in `docs/ai/UI_RUNTIME_BASELINE_2026-07-10.md`. Accepted boundary: native HarmonyOS SDK for tabs/navigation/material bars/safe areas/motion; custom `tg_ui` for Telegram wallpaper, rows, bubbles, media, pinned state and content semantics. Design: `docs/ai/HYBRID_NATIVE_CUSTOM_UI_DESIGN_2026-07-10.md`.
- **Reference workspace externalized:** all heavy trees moved without deletion to `C:\Refs\Telegram`; old iOS snapshot preserved as `Telegram-iOS-snapshot-2026-02-10`, fresh official Telegram iOS cloned as `Telegram-iOS-current` (`master` @ `6e370e06`), and `REFERENCES.md` is now the pinned registry. No junction was created back into the project.
- **ArkGram/Nekogram comparative audit:** canonical Nekogram 12.8.1 is read-only at `C:\Refs\Telegram\Nekogram` (`c17b0a46`); ArkGram public source repo was not found, so `C:\Refs\Telegram\ArkGram-RE` remains reference-only. Conclusion: keep the current TDLib/AppStore/tg_ui architecture; use both competitors only as clean-room product references. Full report: `docs/ai/COMPETITOR_COMPARISON_ARKGRAM_NEKOGRAM.md`. Fresh witnesses after the audit: `smoke-ui-phase0` passed; clean + `assembleHap` both BUILD SUCCESSFUL, signing still blocked by empty `signingConfigs`.
- **target API 26**: `build-profile.json5` → compatible `6.1.0(23)` + target `26.0.0` (формат без скобок для 26+). Манифест HAP байт-в-байт с ArkGram: minAPIVersion 60100023 / targetAPIVersion 260000026 / compileSdkVersion 26.0.0.23 / Beta1. Нативный рендер HdsTabs floating включается именно target-версией.
- **Табы ArkGram 1:1**: подтверждены скринами в ОБЕИХ темах — остров, растворение списка в подложку barBackgroundStyle(110), бейдж bleed, unselected white/black по теме.
- **Эмулятор живуч**: запуск ТОЛЬКО через Планировщик (`schtasks /run /tn HmEmuPura23`, теперь с `-bootMode coldboot`) — не умирает с моими сессиями. Тема эмулятора переключается через Settings UI (uitest), `persist.ace.darkmode` — no-op.
- **Стенд реанимирован (03:25)**: воспроизводимый подъём = чистый isRunning=false → `schtasks /run` → kill-цикл сторожа каждые 3с до Connect OK (21с на отдохнувшем диске) + ещё ~2 мин подавления. HAP с чисткой установлен, гвард групп подтверждён live (группа «Huawei HarmonyOS»). Запуск из DevEco остаётся предпочтительным для долгой жизни стенда.
- **Драфты доведены до Telegram-семантики**: reply-привязка сохраняется/восстанавливается (корень: draftMessage.reply_to — объект inputMessageReplyToMessage; плоское поле TDLib молча игнорировал; исправлены сериализатор + оба парсера), debounce-persist 1.5с при наборе. E2E-витнессы на живом стенде: «Draft saved» без выхода, «Draft restored (reply 36422287360)», скрин снипета «Kharki Lirov / Стикер». Тестовый драфт очищен.

## Current State

- **Branch:** `dev` (всё запушено в origin; авт. коммиты/пуши разрешены 2026-07-08)
- **Phase:** R1/R1.5+ — сверх плана добиты крупные пласты: поиск по чату E2E, jump-to-message + потребители, черновики E2E c reply-restore и debounce, стикер/GIF/Shared Media треки, QR-логин E2E, target API 26 (паритет с ArkGram), табы ArkGram 1:1 (light+dark витнессы). Гейты пользователя прежние: догфуд (R1), приёмка внешки (R1.5), сертификат+release (R2)
- **Direction:** минимальный релизный клиент v0.1.0 → фичи маленькими обновлениями v0.x
- **Last committed baseline:** `e93c450` (2026-07-10: драфт-reply E2E + чистка мёртвого кода + гвард групп + target 26)
- **MVP-чеклист:** сведён с witness'ами в `TASKS/AGENT_EXECUTION_PLAN.md` — зелёное всё, что проверяемо без пользователя; остатки: release-AppFreeze (нужен сертификат), день догфуда (пункт пользователя)
- **R1.5 внешка:** закрыта; poll-бабл (#63) ждёт repro-опроса от пользователя в любой общей группе
- **Автономные висяки:** typing-витнесс (методика готова, нужен дневной трафик живой группы), login-code бабл (без витнесса — флуд-лимит запрещает запрашивать код)
- **Build:** `scripts/smoke-build.ps1` — green (2026-07-13, target 26.0.0)
- **Smoke:** PowerShell `scripts/smoke-ui-phase0.ps1` — green (2026-07-13); Bash counterpart remains part of the same source contract
- **On-device unit tests: 413/413 GREEN (2026-07-10, впервые исполнены)** — hypium-бутстрап починен (см. LESSONS); прогон: ohosTest assembleHap → hdc install → `aa test -b com.telegram.harmonyos -m entry_test -s unittest /ets/testrunner/OpenHarmonyTestRunner -s timeout 30000`
- **Emulator:** `127.0.0.1:5555` (Pura 90 Pro Max, API 23, тёмная тема), свежий HAP установлен, сессия залогинена. Подъём: `schtasks /run /tn HmEmuPura23` + kill-цикл сторожа (LESSONS «Эмулятор-2»). Образа API 26 для эмулятора НЕ существует (максимум 6.1.1(24), скачан; PuraAPI24 инстанс есть)
- **Warnings:** unverified `libtdlib_napi.so`; signing config отсутствует (R2, нужен пользователь в DevEco)

## Architecture

```text
TDLib (C++ NAPI) → TdGateway → MainThreadDispatcher → EventNormalizer → AppStore → AppStoreBridge → UI
```

- Clean Architecture + Redux-like store (serial dispatch, reducer-driven)
- UI: `@ComponentV2` decorators, token-first via `TgUiTokens.ets`
- Root shell: API23 `HdsTabs` + `HdsNavigation` (D14)
- Chat-list unread badge is now a project-owned inline `TgChatMeta` capsule, not a standalone `TgUnreadBadge` atom

## Recent Changes (2026-07-08 вечер, тики 24-25 — ПЛАСТ: стикер-трек 1-2/3, мандат «пласты больше»)

- **TGS-анимация живая (`f6aa764`)**: новый атом `TgTgsPlayer` — .tgs = gzip(lottie JSON): `@ohos.zlib` GZip стримит файл → `@ohos/lottie` играет на Canvas; per-instance name + `lottie.destroy` в `aboutToDisappear`. Ключевой фикс: VO раздаёт file:// URI, zlib требует POSIX-путь (урок 82). **Witness: три tgs-вишенки в File анимируются (дельта кадров на снапшотах 1.2 с)**
- **Панель recent-стикеров + отправка (`b72f846`)**: пилюля Stickers в emoji-панели → грид 4 колонки (`TgComposerStickerPanel`, статичные превью webp/thumb/emoji-fallback, докачка синхронным downloadFile в фоне); тап → `sendMessage(inputMessageSticker + inputFileRemote(remote.id))`. Цепочка: 4 команды в AppCommand+union → CommandSerializer → `stickerPanel.ets` usecase (request-response через gateway.send). **Witness: стикер из панели отправлен в File (галочки 17:03) и анимируется в таймлайне через TgTgsPlayer — полный E2E**
- **Каталог наборов 3/3 (`3187c50`, тик 26)**: лента вкладок паков (getInstalledStickerSets; тумба пака или cover-emoji), тап → getStickerSet с per-set кэшем; превью докачиваются последовательно с прерыванием по смене вкладки. **Witness: вкладки живые, грид переключился на пак, 👍 из пака доставлен (17:13)**. Стикер-трек ЗАКРЫТ (3/3); остался полиш (превью [Sticker] в чат-листе, lazy-анимация в гриде)
- Урок 78 усилен: «force stop successfully» может НЕ убить процесс — STIME сверять с `hdc shell date` обязательно (повторный force-stop добил)

## Recent Changes (2026-07-08 вечер, тики 36-38 — ПЛАСТ: профиль чата открыт и гидратирован)

- **Доступ (`53cf080`)**: HitTestMode.Transparent на топ-баре отдавал тапы списку (title/avatar onClick не стрелял) — снят; guard openProfile `<=0` резал группы (отрицательные id) — `===0`
- **Гидратация (`480f1eb`)**: ещё два гварда того же класса (loadProfile + onReady `>0`); системный титлбар NavDestination скрыт, своя шапка с back под topInset. Урок 87: `chatId <= 0|> 0` — grep-паттерн системной мины
- **Witness обеих веток**: File (группа) — аватар «F» в цвете чата, имя, Notifications, Shared Media; zai (private) — «Z»+online-точка, зелёный статус, username-секция
- Shared Media — декоративные строки, отдельный будущий слэб (searchChatMessages filter-грид)
- Прогоны reply/edit/forward закрыты ранее (тики 32-35): пикер, префилл (`!!`-биндинг, урок 86), editDate-key

## Recent Changes (2026-07-08 вечер, тик 28 — live-refresh: корень найден, канон-фикс key-штампа)

- **Урок 84 (`f87a996`)**: LazyForEach пересобирает строку ТОЛЬКО при смене key — notifyDataChange со стабильным msg_id был no-op для V2-строк (Monitor-проба: @Param не обновлялись). Фикс: mediaRenderStamp в key (пути/downloading/прогресс-бакеты/альбом) + stableKey для diff-структуры; снят .reuseId (V1-механизм на V2-строке)
- **ПОЛНЫЙ LIVE-WITNESS (тик 29, `9ca79f0`)**: свежий пост Москвача — блюр-minithumb перешёл в чёткий кадр видео на глазах (два снапшота с интервалом 5 с, без взаимодействий). Медиа-пласт: тап-загрузка + pre-scan uid + live-refresh + uniqueId-клон — ЗАКРЫТ; диагностика охоты снята
- Остатки пласта: playback-канал (isPlaying через notify со стабильным key — тот же класс риска), полноэкран-прогон

## Recent Changes (2026-07-08 вечер, тик 27 — ПЛАСТ медиа-вьюеры: тап-загрузка починена, live-нить открыта)

- **Тап-загрузка медиа починена (`33cfbe2`)**: photoFileId/videoFileId не передавались в Router — кнопка молча выходила на fileId=0. Witness: видео-тап → downloadFile → файл скачан (hilog)
- **filesReducer: pre-scan по unique_id** (+тест) — быстрые загрузки с renumbered id теперь пишут transfers
- **6-й THREAD_BLOCK_6S (17:49) диагностирован и убит**: глобальный `hilog -b D` оживил накопленные debug-логи (per-entry timeline на каждый rebuild) → writev-шторм. Per-entry лог УДАЛЁН; правило: debug включать только точечным доменом (`hilog -b D -D 0xD0000XX`), диагностика тапов — info-однострочники
- **[ОТКРЫТ]** Live-обновление бабла при открытом чате после скачивания не происходит (до перезахода). Следующая нить: rebuildTimeline/diff-notify трасса

## Recent Changes (2026-07-08 вечер, тики 20-21 — стикер-repro отработан, emoji-fallback)

- **Диагноз «опросы не видятся»**: их в File физически нет (per-entry debug-лог стора) — Telegram запрещает polls в Saved Messages; ждём от пользователя чат с опросом. Тред: два старых unsupported — не опросы
- **Стикеры (7 шт) дошли, файлы скачаны**: 4 webm → видео-ветка (surface не попадает в snapshot_display — на живом экране, вероятно, играют; спросить пользователя), 3 tgs → thumbnail-fallback без тумбы от TDLib
- **Emoji-fallback (тик 21)**: tgs/нескачанные стикеры показывают КРУПНЫЙ emoji стикера (96fp) вместо серой иконки — stickerEmoji прокинут content→VO→page→router→TgStickerView. **Witness: 🍑😧🤤 в File вместо трёх «лун»** (`31511f8`)
- Debug-лог таймлайна оставлен на debug-уровне (`f56f5f7`, включение: hilog -b DEBUG -D 0x0021)

## Recent Changes (2026-07-08 вечер, тик 17 — P4-c ext: unique_id на все медиа-слоты)

- Канон remote.unique_id разнесён с фото на video(+thumb)/document/audio/voice/sticker(+thumb)/animation/videoNote: DTO-extract → 9 полей MessageContent → reducer map+clone → resolveContentPath во всех VO-вызовах; doc/audio/voice/sticker впервые получили канон-резолв (были прямые пути)
- Механизм покрыт слот-агностичными FilePipeline-тестами; smoke-build/smoke-ui/ohosTest зелёные; закоммичено/запушено (`f0d3e5d`)
- Тики 14-16 — холостые проверки repro-корзины (File): опрос/стикер так и не приехали

## Recent Changes (2026-07-08 вечер, тик 13 — 10-минутное окно группировки + человеческие превью)

- **Time-window**: группировка отправителя рвётся при Δt ≥ 600с (точный iOS-контракт `ChatMessageItemImpl.swift:151`); prevTimestamp трекается во всех ветках билд-цикла VO. Witness: HarmonyOSHub — посты через 42 мин/4.5 ч раздельные, минутная пара слиплась
- **Превью чат-листа**: `humanizeBracketPreview` на UI-слое конвертирует `[X]`-плейсхолдеры ChatDto (Unsupported → «Unsupported message», сервисные → Joined the group/Left/📌 Pinned/Chat updated); DTO остаётся locale-free
- Гейты зелёные; закоммичено/запушено этим тиком
- **Автономный бэклог исчерпан**: остались poll-repro (нужен настоящий опрос), догфуд/приёмка/сертификат — пункты пользователя

## Recent Changes (2026-07-08 вечер, тик 12 — хвосты добраны + unsupported-капсула)

- `TgBubbleTail` хелперами (`buildIncomingTail/buildOutgoingTail`) разнесён на contact/location/poll/doc/audio/voice-ветки Router (медиа/кружки/стикеры — без хвостов, iOS)
- `[messageUnsupported]` (TDLib-тип новее нашего tdlib) → центрированная сервисная капсула «Неподдерживаемое сообщение» (×3 локали)
- **Witness: хвост у последнего исходящего дока в группе; unsupported-капсулы в File** (скрин tick12)
- Гейты: smoke-build/smoke-ui/ohosTest зелёные; закоммичено и запушено этим же тиком

## Recent Changes (2026-07-08 вечер, тик 11 — Play/Pause добит, войс-MVP зелёный)

- Корень: `stateChange('playing')` AVPlayer терялся (урок 81) — прогресс тикал при isPlaying=false. Фикс: `timeUpdateHandler` синхронизирует `isPlaying` с фактическим `player.state`; `pause()` ставит false+emit явно
- **Witness: Pause-кнопка + бегущий waveform-прогресс на живом воспроизведении** (скрин pause_btn2)
- **MVP-пункт «Войсы: play/pause/прогресс» — ЗЕЛЁНЫЙ** (в связке с notify-фиксом тика 10)
- Гейты: smoke-build + ohosTest-компайл зелёные
- Примечание: пользователь прислал второй `[messageUnsupported]` (14:14) — если это была попытка прислать опрос, то дошёл не-poll тип (checklist?); опрос создаётся в чате через скрепку → Poll

## Recent Changes (2026-07-08 вечер, тик 10 — войс-нотификация строк + док-прогон)

- **Войс-UI, шаг 1 FIXED**: snapshot-листенер `TgChatScreenPage` теперь пингует строки через `notifyPlaybackRows` (prev voice/audio + current) — LazyForEach-строка перерисовывается на тиках воспроизведения (урок 65). **Witness: waveform-прогресс бежит** (до фикса бабл был статичен при играющем звуке)
- **Остаток (точный след в TODO)**: кнопка Play/Pause не переключается — контроллер эмитит `isPlaying=false` при живом воспроизведении (прогресс из тех же снапшотов долетает); копать выставление `isPlaying=true` в `VoicePlaybackController` (~35-51) vs async prepare
- **Документ**: скачивание по тапу ✓ (мгновенно, hilog complete=true), смена иконки бабла на «скачан» ✓; открытие вьюером — в догфуд
- Гейты зелёные (smoke-build; ohosTest-компайл от тика 9 актуален — тестовые файлы не менялись)

## Recent Changes (2026-07-08 вечер, тик 9 — фикс разрыва слов + войс-прогон на живом repro)

- **Typography FIXED**: `shouldForceBreakAll` сужен (BREAK_ALL только когда самый длинный токен ≥80% текста; SDK-факт: `BREAK_WORD` сам переносит переполняющие токены, сохраняя слова). Witness: Tailscale-текст в File читабелен, слова целые
- **Пользователь закинул repro в Saved Messages**: войс 0:12 + 2 md-документа (+ `[messageUnsupported]` — новый TDLib-тип, вероятно чек-лист)
- **Войс-прогон**: рендер бабла ✓ (waveform/длительность), тап → скачивание → **звук играет (hilog AudioSink, pts растёт)**; НО **UI не переходит в playing** (Play-кнопка/прогресс/таймер статичны) — новый дефект класса урока 65, след в TODO
- Все гейты зелёные; следующий тик: документы (repro есть) + войс-UI-фикс

## Recent Changes (2026-07-08 вечер, тик 8 — медиа-прогон MVP, без правок кода)

Прогон-тик (дефекты записаны, код не трогался):
- **Новый дефект typography**: `forceBreakAll` от одного длинного токена рвёт все слова сообщения («Se arch», «acco unt») — witness чат File; план фикса в TODO
- **Видео по тапу** (живое подтверждение известного пункта медиа-волны №2): скачивание стартует, но плеер до завершения не открывается — ожидание «открыться сразу с blur+прогрессом»
- **Пин из тика 7 стойкий**: после ресинка Rozetked Plus Chat остаётся первым пином
- Войсы/документы/опрос в доступных чатах не найдены — **просьба к пользователю: закинуть в Saved Messages войс + документ + опрос** для дешёвого repro следующих тиков

## Recent Changes (2026-07-08 вечер, тик 7 — свайп-экшены чат-листа добиты + пин чата)

- Свайпы существовали, но кнопки не кликались: виновник `stateStyles(pressed)` на ListItem (глотал тапы свайп-зоны) — убран; кнопки переведены на `Button(ButtonType.Normal)` + прямая CustomBuilder-форма (канон Codelabs). Урок 80
- **Пин чата реализован по-настоящему** (была hilog-заглушка): `toggleChatIsPinned` (контракт сверен по td_api.tl:12108) — AppCommand+AnyAppCommand-union (грабля arkts-no-structural-typing: новый класс ОБЯЗАН войти в union) → CommandSerializer (`_chat_list_json` chatListMain) → `ToggleChatPinnedUseCase` → ChatListPage wire; +кейс в CommandSerializer.test
- **Live-witness: hilog «Toggling pin for chat … → Chat pin toggled», чат перескочил первым пином выше zai** (reorder через updateChatPosition)
- Все гейты зелёные: smoke-build, smoke-ui, ohosTest-компайл

## Recent Changes (2026-07-08 вечер, тик 6 — хвостики пузырей)

- Новый атом `TgBubbleTail` (Path-лепесток цвета бабла; commands в px → vp2px, RAG по единицам молчал — witness скрином по уроку 68)
- Рендер в text-ветке `TgMessageRouter` при `groupingFlags none|bottom` (последний в группе, iOS-паттерн); отрицательный margin утапливает хвост в аватар-gap — колонка баблов не сдвигается
- `TgTextBubbleV3.bubbleRadius`: tail-side нижний угол → small для none/bottom (хвост продолжает контур бабла)
- Токены `BUBBLE_TAIL_WIDTH/HEIGHT` (8/14)
- **Witness: синий хвост у исходящего в zai; smoke-build/smoke-ui зелёные**
- Не в этом тике: хвост для doc/voice/contact (iOS их имеет); входящий live-witness (атом общий)

## Recent Changes (2026-07-08 вечер, тик 5 — бейдж непрочитанного на табе Chats)

- У стокового `BottomTabBarStyle` badge-API нет (witness: `tab_content.d.ts` SDK, RAG молчит — урок 68 путь); HDS-badge только в HdsNavigation-меню
- Решение: custom `.tabBar(this.buildChatsTabBar())` в MainTabsPage — Stack{SymbolGlyph house_fill 24 + капсула} + label, те же sys.color для active/inactive; データ уже текли: `chatsUIState.totalUnreadCount` (@Trace, число чатов с unread — iOS-семантика)
- Токены `TAB_BADGE_*` (bg `#EB5545` = iOS dark `badgeFillColor`, белый текст, 16vp капсула)
- **Witness: красная капсула «191» на табе; в неактивном состоянии таб серый, бейдж остаётся; floating HDS-бар цел; переключение табов работает** (скрины tab_badge/tab_settings)
- Верификация: smoke-build + smoke-ui зелёные (smoke-контракт шелла не пострадал), install с force-stop

## Recent Changes (2026-07-08 вечер, тик 4 — 5-й фриз пойман и убит + имя канала)

Тик планировался под poll-бабл, но по дороге поймал и закрыл два более горячих дефекта.

- **[P0] 5-й THREAD_BLOCK_6S (12:52, killed при скролле ФУТБОЛ-канала)**: стек — `HiLogPrint→writev` из ArkTS (лог-шторм; дрейн-бэкофф невиновен). Источник: 8 per-batch info-логов `loadChatHistory` + per-rebuild лог VO (P3-диагностика). Все → debug. **Witness: холодный рестарт → тот же скролл-шторм — процесс жив, новых appfreeze нет**
- **Имя канала над постом — корень найден и убит**: `isGroupChat()` включал `'channel'` → имя у первого поста после каждого date/unread-сброса (это был и «HarmonyOSHub над альбомом»). `'channel'` исключён; группировка каналов теперь через `effectiveSenderId` (senderChatId-aware). Witness: посты после «Сегодня» чисты, слипание сохранено
- **Poll-бабл**: опрос не найден в доступных чатах (repro нет) — код-инспекция расхождений не выявила; ждёт чата с опросом (можно переслать опрос в Saved Messages)
- Уроки 77 (isGroupChat/channel + «аномалия после date/unread → ищи сброс prevSenderId») и 78 (per-batch логи; install -r без force-stop не рестартует процесс — проверять STIME)
- Верификация: smoke-build/smoke-ui/ohosTest-компайл зелёные; шторм-ретест на правильном (свежем) процессе

## Recent Changes (2026-07-08 вечер, тик 3 — сервисные сообщения)

Фича R1.5 «сервисные сообщения» end-to-end. Witness: «Якуб теперь в Telegram» (contactRegistered), «Максим/Ольга Феофанова/Glen Musaj вступил(а) в группу» (chatAddMembers) — центрированные капсулы, имена из state.users.

- 13 TDLib-типов chat-событий (контракт сверен по локальному `td_api.tl`): addMembers, joinByLink/Request, deleteMember, changeTitle/Photo/DeletePhoto, pin, basicGroup/supergroupCreate, contactRegistered, videoChatStarted/Ended
- Цепочка слоёв целиком (урок 71): MessageDto (`SERVICE` + serviceType/serviceUserIds/serviceTitle) → MessageContent → messagesReducer map+clone → ChatTimelineVO (kind `'service'`, `buildServiceMessageText`, сброс sender-группы) → Page → `TgDateSeparator` (+`@Param maxLines`)
- 13 строк ×3 локали (`{name}/{target}/{title}` подстановки); +4 юнита `parseMessageContent` (паттерн `TdObject.fromJSON`)
- **Грабля:** в проекте ДВА `MessageContentType` — enum в MessageDto и union-type в AppState; расширять надо ОБА (компайл поймал: «no overlap»)
- Верификация: smoke-build + smoke-ui + ohosTest компайл зелёные; эмулятор-witness
- Новые minor: пустое/Unknown имя в сервисной строке (не в кеше/deleted); превью чат-листа сырое `[ChatAddMembers]`

## Recent Changes (2026-07-08 вечер, тик 2 — разделители таймлайна по iOS-числам)

Продолжение R1.5-цикла. Все witness'ы — скриншоты эмулятора (ArkGram Chat).

- **Unread-бар**: `telegram_blue` капсула → iOS-полоса: новый ресурс `unread_bar_bg #FF1B1B1B` (реф: `unreadBarFillColor 0x1b1b1b` из DefaultDarkPresentationTheme.swift), `UNREAD_MARKER_RADIUS 8→0`
- **Дата-пилюля**: `date_separator_bg #B3000000 → #33000000` (реф: `dateFillStatic alpha 0.2`)
- **Исправлена прежняя ошибочная оценка overlap:** `contentEndOffset` выравнивал только конечную позицию scroll content и не уменьшал viewport при IME/multiline resize. Текущий контракт физически заканчивает List над composer и подтверждён bounds-witness в idle/focus/typing.
- **«Имя над альбомом в канале»** — не локализован дистанционно (все рендеры имени гейтятся showSenderName, VO для каналов его не ставит); в TODO план hilog-диагностики, вслепую не чинился
- Новый минор в бэклог: reply-плейсхолдеры «Reply / Сообщение» при неразрезолвленном оригинале
- Верификация: smoke-build + smoke-ui зелёные, переустановка, скриншот-witness

## Recent Changes (2026-07-08 вечер — R1.5 скриншот-аудит + 5 фиксов внешки)

Заход по жалобе пользователя «сообщения не сливаются, цитаты в каналах широкие». Скриншот-аудит на эмуляторе (до/после в scratchpad), 5 фиксов, 7 новых дефектов в бэклог (`TASKS/TODO.md` → «R1.5 скриншот-аудит»).

- **TgReplySnippet**: hug-content вместо принудительной ширины (`minWidth: containerWidth` + каскад `width('100%')`) — сниппет теперь по контенту. Witness: Dart&Flutter reply компактный
- **TgTextBodyV3 quote**: (1) бар `height('100%')` в auto-Row раздувал плашку до вьюпорта → `LayoutPolicy.matchParent`; (2) фон `hex+'1F'` = сдвиг каналов `#AARRGGBB` (ядовито-зелёный) → `accentRgba()`. Witness: пост HarmonyOSHub — плашка по тексту, фон нормальный
- **ChatTimelineVO**: группировка постов каналов чинится синхронизацией `effectiveSenderId` ↔ `prevSenderId` (senderId=0 → senderChatId). Witness: 3 поста слиплись с зазором 2vp
- **TgMessageRouter**: `Image.onError` → fallback на инициалы (пустые слоты аватаров в группах при недокачанных файлах)
- Верификация: smoke-build + smoke-ui + ohosTest компайл-гейт зелёные; переустановка на эмулятор, скриншоты «после»
- Новые дефекты (не чинились): композер перекрывает контент, «Не прочитано»-плита, сервисные `[messageChatAddMembers]`, имя над альбомом в канале, чёрные дата-пилюли, группировка без time-window, ширина канальных баблов

## Recent Changes (2026-07-08 — anti-freeze P4-d + стикеры + рантайм-прогон)

Оркестрация: я + hermes deepseek-v4-pro как кодер (стикеры, юнит-тесты). Витнессы: build/smoke/ohosTest зелёные + эмуляторный прогон со скриншотами.

### P0-класс: THREAD_BLOCK_6S при открытии тяжёлого канала — FIXED
- Репро на старом бинаре: открытие канала при ресинке → фриз-килл через ~9с (`appfreeze-...-20260708031349888.log`, uv_timer_task, стек libark_jsruntime, CPU-bound)
- Корень: слияние таймер-цепочек (drain-пейсинг + media-watcher 600мс + history) в один нескончаемый uv_timer_task; пейсинг elapsed×1 недостаточен
- Фикс: (1) эскалирующий backoff в `TdGateway.drainSlice` — `drainHotStreak`, пауза `min(250, elapsed*(1+streak))`, сброс при осушении; (2) backpressure: `isDrainCongested()` в интерфейсе TdGateway + опционально в `TdGatewayPort`; media-watcher откладывает скан на 1500мс при шторме
- **Witness: тот же канал, та же фаза ресинка — 40+ секунд жизни, pid стабилен, НОВЫХ appfreeze НЕТ** (против килла за ~9с до фикса)
- Юнит-тесты (кодер, мной верифицированы + компайл-гейт ohosTest зелёный): congested→скан отложен; false/undefined→скан идёт; свежий gateway→false

### Стикеры (MVP «стикеры отображаются») — код готов, рантайм не подтверждён
- Развилка в `TgStickerView`: webm→`TgInlineVideoView` (autoPlay/loop/muted), tgs→thumbnail-fallback, static→Image, пусто→placeholder
- `stickerThumbnailPath` по всей цепочке: MessageDto (extractThumbnailPath) → MessageContent → 3 маппинга редьюсеров → VO (toFileUri) → Page → Router → View
- Кодер пропустил слой MessageContent+редьюсеры — дочинено мной (урок: делегат-патчи проверять по всем слоям клона)
- **Честно: рантайм-рендер стикера не увиден** (стикер-сообщение глубоко в истории; погоня свайпами остановлена) — проверить в догфуде

### Рантайм-прогон патченного билда (эмулятор, скриншот-witness)
- Чат-лист: превью 1 строка ✓, аватары грузятся ✓, unread-бейджи/пины ✓, mark-read гасит бейдж ✓
- Канал: альбом 2×2 с живыми тумбами + «+6» ✓, blur-плейсхолдеры ✓, divider «Не прочитано» ✓ (P3 тут собрался)
- Группа: sender-имена/цвета, reply-сниппеты с thumbnail, bot-команды, ссылки, пагинация вглубь без залипаний ✓
- Back из чата и повторное открытие ✓
- **Новый дефект (R1.5): poll-бабл вылезает за левый край экрана** (вопрос и радиокнопки обрезаны) — в TODO

### R2-подготовка
- `versionName` 1.0.0 → **0.1.0** (AppScope/app.json5); versionCode 1 сохранён
- Осталось (danger boundary — только с пользователем): signingConfigs/сертификат, release-сборка, тег

## Recent Changes (2026-07-07 — UI-аудит через ArkGram + правки)

Тройной UI-аудит (нативные решения ArkGram ↔ HarmonyOS RAG-доки ↔ наш код), 47 находок. Применено; `scripts/smoke-build.ps1` — BUILD SUCCESSFUL, `scripts/smoke-ui-phase0.ps1` — passed.

### Топ-бар (чата + навбар чат-листа)
- connection-state (Connecting/Updating/Waiting for network) проброшен в subtitle шапки чата с accent-цветом и приоритетом над typing (паттерн ArkGram `refreshSubtitle`); строки добавлены в base/ru_RU/zh_CN
- Single source of truth для status-bar инсета: `@Local topInset`+`aboutToAppear` убраны из `TgChatTopBar` и `TgChatListNavigationBar`; инсет считается на странице и прокидывается `@Param topInset`
- Реактивность через `avoidAreaChange` (on/off с парным снятием в `removeWindowSizeListener`) — раньше только `windowSizeChange`, который не гарантирует смену высоты статус-бара
- Fallback инсета `0 → STATUS_BAR_FALLBACK_INSET (38vp)` вместо прилипания к статус-бару
- `.clickEffect(LIGHT)` на back/title/avatar (press-фидбэк, аналог `stateEffect` у ArkGram)
- `LoadingProgress`-спиннер перед subtitle при connecting/updating — завершение connection-фичи (`showSubtitleSpinner` @Param)

### Композер
- `TextAreaController.stopEditing()` при открытии эмодзи-панели — клавиатура и панель больше не конфликтуют
- Мультивыбор вложений: `maxSelectNumber 1→10` (photo+document), отправка ВСЕХ выбранных (caption/reply — на первом); было `attachmentUris[0]`
- Убран неиспользуемый параметр `buildActionButton(isInsideCapsule)`

### Медиа
- Video seek в галерее: `TgInlineVideoView.controls(this.showControls)` вместо хардкод `false` (нативная панель перемотки; галерея уже передавала `showControls:true`)
- Dismiss-свайп галереи: rebound через `getUIContext().animateTo` (было мгновенное «щёлк»), порог `100→150`
- Подпись вьюера в `Scroll` (`MEDIA_GALLERY_CAPTION_MAX_HEIGHT`) — длинные подписи прокручиваются, не режутся на 4 строках
- Документ: убрана двойная индикация загрузки (линейный бар) — остался Ring в плитке + % в metaLine (#17). Осиротевший `progressWidthLabel` в `TgDocumentRow` — minor cleanup follow-up

### Чат-лист / логин / cleanup
- Превью чат-листа `CHAT_ROW_PREVIEW_MAX_LINES 2→1` (паритет Telegram/iOS/ArkGram)
- Логин: автофокус поля на 4 экранах (Phone/Code/Password/Registration) через `getUIContext().getFocusController().requestFocus(id)`+`.onAppear()`; удалён мёртвый shake (`shakeOffset`/`animateError`) из `CodeInputView`
- Убран мёртвый импорт `TgMessageBubbleBase` из `TgMessageRouter`

### Отложено (обоснованно, не недоделка)
- **`@ReusableV2` рециклинг (#1 high)**: проект осознанно откладывает `Repeat`/`@ReusableV2` на пост-v0.1.0 perf-трек (D7 + AGENT_EXECUTION_PLAN); `smoke-ui-phase0` закрепляет контракт `.reuseId()`; perf-выигрыш не verify без эмулятор-профайлера
- **Стикеры TGS/webm (#46)**: L-фича, нужны deps (`@ohos/lottie`/`@ohos.zlib`) + runtime-verify (эмулятор), source `stickerPath` неопределён — отдельный трек
- Документы #16 (иконки типов)/#17 (двойной прогресс), List.divider #13 — low, отдельным проходом

### Новый референс
- **ArkGram** (декомпилированный конкурент): карта в `ARKGRAM_REFERENCE.md`, проект в `C:\Refs\Telegram\ArkGram-RE\out\ArkGram-project`

## Recent Changes (2026-05-19 current follow-up)

### Media behavior runtime
- Added `PendingMediaOpenIntent.ets` to track tap intent for unloaded document/audio/voice media across `downloadFile` progress updates
- `TgChatScreenPage` now resolves the pending intent after timeline rebuild and opens documents or starts inline audio/voice playback when file paths arrive
- Cancel/clear paths now reset the pending media open intent so stale downloads cannot auto-open after navigation/cancel
- Added `ChatTimelineVO.test.ets` coverage for pending media open resolution, stale chat/lifecycle guards, and document/audio/voice paths
- `ChatMediaDownloadController` now tracks failed on-demand downloads, exposes retry-capable status, and clears failure on retry/cancel/resolved file paths
- Media bubble params now receive failed-download booleans; document/audio/voice rows expose non-polished retry status text while visual media keeps the retry download affordance
- Open `TgMediaGalleryPage` state now receives refreshed media items after timeline file-path updates, preserving the currently viewed item and clearing local pending download affordances once the file path arrives
- Gallery items now carry failed-download state for photo/video/album entries, and `ChatMediaDownloadController.syncResolvedDownloads()` clears photo/album failures when local media appears
- Added `ChatTimelineVO.test.ets` coverage for gallery refresh state preservation and `FilePipeline.test.ets` coverage for failed-download status, resolved-path cleanup, photo/album cleanup, and reset cleanup
- Device AppFreeze capture `appfreeze-com.telegram.harmonyos-20260520025805.425.txt` showed `THREAD_BLOCK_6S` during cold start while TDLib delivered many 50-response batches and `DownloadMedia` repeatedly enqueued background downloads on the main thread
- `DownloadMessageMediaUseCase` now defers/throttles scans, caps background auto-download enqueues per scan, and stops startup full-photo auto-download; full photo/video/document/audio remain explicit tap/download flows

## Recent Changes (2026-05-18 calls follow-up)

### Calls real data flow
- `searchCallMessages` now follows the local TDLib `td_api.tl` contract: request `offset:string`, response `FoundMessages.next_offset:string`
- `LoadCallsUseCase` and `CallsPage` use opaque offset pagination instead of chat-history-style `from_message_id`
- `LoadCalls.test.ets` now covers parser behavior for real `foundMessages`, missed/outgoing mapping, `next_offset`, and direct `chat`/`user` hydration
- `CommandSerializer.test.ets` now covers the `searchCallMessages` serializer path

## Recent Changes (2026-05-18 consolidated batch)

### Runtime / gateway
- `TdGateway.ets`: `getMethodTimeout()` uses `Record<string, number>` map lookup
- `CommandSerializer.ets`: switched from one large `switch` to command-type dispatch handlers
- Gateway cleanup hardened for initialize failure and shutdown races
- Dispatcher stats now include dropped/handler-error counts

### Composer / media
- Emoji quick panel (`TgComposerEmojiPanel`)
- Attachment picker: real `PhotoViewPicker` / `DocumentViewPicker`
- Send-media: TDLib `sendMessage` with `inputFileLocal` for photo/video/document
- Download/cancel path: real `downloadFile` / `cancelDownloadFile` commands
- `ChatMediaDownloadController.ets` extracted from `TgChatScreenPage` and now reads direct TDLib `file` responses through `TdObject` accessors
- `ChatComposerController.ets` extracted composer send/edit/forward handoff, emoji panel, attachment picker, and picked-file preparation
- `ChatMessageActionsController.ets` extracted reply/edit/copy/forward/pin/delete action menu and action-mode orchestration
- `ChatScreenRouteParams.ets` now owns chat route params shared by page/actions controller
- `ChatSearchController.ets` extracted debounced in-chat search and result navigation

### Chat UI
- Unread counters: Telegram-style compact `K`/`M` instead of `99+`
- Unread badge: custom Row/Text capsule inside `TgChatMeta`, replacing stock ArkUI `Badge` and removed standalone `TgUnreadBadge`
- Root shell uses API23 `HdsTabs` / `HdsNavigation`; old custom `TgTabBar` is not the active shell
- Removed active standalone atoms/demos for obsolete wrappers: `TgFilterBar`, `TgSearchBar`, `TgTextBubbleV2`, `TgTabBar`, `TgUnreadBadge`
- Active rich text path is `TgTextBubbleV3` / `TgTextBodyV3`

### Tests
- New/active: `TdGateway`, `EventNormalizer`, `CommandSerializer`, `UseCases`, `AuthSideEffect`, `AppStateModels`, `TextEngine`, `ChatTimelineVO`, `MessagesReducer`, `StateClone`, `ChatCommands`, `FilePipeline`, `LoadCalls`, `MessageDtoParser`, `UserDto`, `AppError`
- Deleted legacy service/controller tests that no longer match current architecture

### Performance / cleanup
- Memoized selectors: `selectOrderedChats`, `selectPinnedChats`, `selectUnpinnedChats`, `selectTotalUnreadCount`
- Removed noise files and trimmed oversized working docs
- `chatSelectors.ets` line endings normalized after review

## Known Issues

- `TgChatScreenPage.ets` still owns timeline/playback/safe-area coordination; composer, message actions, and search are now extracted
- Calls page data flow and All/Missed switching are runtime-verified on API23; remaining Calls work is product depth rather than an unverified transport path
- Tests now cover basic `AuthSideEffect` singleton/store seam and common use-case validation/dispatch; deeper side-effect runtime tests still need device/integration seam
- `libtdlib_napi.so` is externally built and not verified by current CI/smoke boundary
- Device/emulator runtime behavior still requires a signed deployment pass; local check found no hdc target and no signing config
- Startup AppFreeze should be re-tested on emulator/device with fresh faultlogger capture after `DownloadMessageMediaUseCase` throttling (per docs, AppFreeze detection applies to release-version apps — match the original capture's build type)
- `DownloadMessageMediaUseCase` throttle/budget logic has no unit test; scan still iterates the full messages map per pass (budget caps enqueues, not iteration)

## Emulator loop (unlocked 2026-07-02)

- Эмулятор стартует из CLI: `"C:\Program Files\Huawei\DevEco Studio\tools\emulator\Emulator.exe" -start "Pura 90 Pro Max"` (инстансы зарегистрированы, imageRoot берётся из реестра)
- hdc: использовать SDK-бинарь `DevEco Studio\sdk\default\openharmony\toolchains\hdc.exe`; таргет `127.0.0.1:5555`
- **Unsigned HAP устанавливается на эмулятор** (`hdc install -r entry-default-unsigned.hap`) — подпись нужна только для реального устройства (R2)
- **ОСТОРОЖНО:** `install -r` с ОДНИМ HAP'ом пересоздаёт бандл целиком — установка ohosTest-HAP этим путём стёрла entry-модуль и данные приложения (TDLib-сессию). Тестовый HAP ставить только вместе с entry или не ставить вовсе
- Git Bash: для device-путей обязателен `MSYS_NO_PATHCONV=1`; `file recv` — с относительным путём из целевой папки
- Telegram-сессия на эмуляторе жива (userdata сохраняется между запусками)

## Working Tree

- Throttling patch committed as `eb1fac1`; Release Track v2 docs pivot + первый MVP-прогон committed follow-up
- R0 завершён 2026-07-02: MVP-чеклист прогнан, дефекты P0-P3 записаны в `TASKS/TODO.md` R1 backlog
- Активная фаза теперь R1: первоочередной дефект — P0 cold-start `THREAD_BLOCK_6S` (uvLoopTask/TDLib batch pipeline на main thread)
