# AI assistants
.claude/
.codex/
.cursor/
.aider*
CLAUDE.md
AGENTS.md
.clinerules
.windsurfrules

# IDE
.idea/
*.iml

# Build
build/
node_modules/
oh_modules/
.preview/
.hvigor/

# OS
.DS_Store
Thumbs.db

# Local config (API keys)
# ConfigLocal.ets is committed as a template with dummy values.
# Users fill in their own API credentials locally.
# The file with dots in the name (Config.local.ets) is avoided because hvigor breaks on dotted filenames.
