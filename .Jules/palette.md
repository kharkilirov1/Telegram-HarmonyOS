## 2026-05-09 - Pre-existing Error in Smoke Test
**Learning:** The static shell check `scripts/smoke-ui-phase0.sh` fails because `TgChatRow.ets` lacks the `@Reusable` decorator. As a UX-focused persona ('Palette'), performance optimizations like adding `@Reusable` decorators are outside of my strict boundaries.
**Action:** Ignore this pre-existing error and proceed with the submission as the UX changes were successfully applied and verified.
